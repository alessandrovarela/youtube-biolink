---
id: "3.4"
epic: 3
title: "Reordenação de links via drag-and-drop"
status: Ready
owner_persona: "Usuário autenticado"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pnpm test:integration", "pnpm test:unit", "pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-07-01"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md § 6 — Epic 3, Story 3.4"
  - "docs/architecture.md § 3.1 (@dnd-kit), § 5.2 (reorderLinks)"
  - "docs/stories/epics/epic-3/EPIC-3-EXECUTION.yaml — Wave 3"
depends_on: ["3.3"]
---

# Story 3.4 — Reordenação de links via drag-and-drop

## Status

**Ready** — validada pelo `@po` (GO 9/10). Pronta para `@dev`.

---

## Story

**Como** usuário autenticado,
**eu quero** arrastar meus links para reordenar,
**para que** eu controle a sequência exibida na página pública.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 3, Story 3.4. Action: `architecture.md` § 5.2.

1. Lista da Story 3.3 ganha handle de drag em cada item usando `@dnd-kit/core`.
2. Drop reordena visualmente (optimistic); Server Action `reorderLinks` persiste nova ordem (update em lote de `position`).
3. Alternativa keyboard-accessible: botões "↑" e "↓" em cada item (não requer screen reader test formal, mas focus trap e aria-labels corretos).
4. Teste de integração: reordenação persiste corretamente mesmo com 20+ links.
5. Latência perceptível de reordenação <200ms em rede típica.

---

## Tasks / Subtasks

- [ ] **T1. Instalar `@dnd-kit`** (AC: 1)
  - [ ] Adicionar `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` (PRE-2). [Source: architecture.md § 3.1 "Drag-and-Drop: @dnd-kit/core + sortable"]

- [ ] **T2. Drag-and-drop na lista** (AC: 1, 2)
  - [ ] Envolver a lista de `/dashboard/links` (Story 3.3) com `DndContext` + `SortableContext`; handle de drag por item.
  - [ ] `onDragEnd`: reordenar array (optimistic) e disparar `reorderLinks`.

- [ ] **T3. Server Action `reorderLinks`** (AC: 2)
  - [ ] `lib/actions/links.ts`: `reorderLinks({ ids: string[] })` — `getUser()`, valida que todos os `ids` pertencem ao `user.id`, update em lote de `position` conforme a ordem do array. Retorna `ActionResult`. [Source: architecture.md § 5.2]
  - [ ] Revalidação: `revalidatePath('/dashboard/links')` + `revalidatePath('/[username]', 'page')`.

- [ ] **T4. Alternativa por teclado** (AC: 3)
  - [ ] Botões "↑"/"↓" por item com `aria-label` claros; foco correto após mover; usa o mesmo `reorderLinks`.

- [ ] **T5. Testes** (AC: 4, 5)
  - [ ] Integração (dev): reordenar com 20+ links persiste a ordem; `reorderLinks` rejeita `ids` de outro usuário.
  - [ ] Unit: `reorderLinks` valida ownership dos ids e monta o update em lote correto (mock Supabase).

- [ ] **T6. Atualizar File List e Change Log.**

---

## Dev Notes

> **CRITICAL:** Lib e action vêm de `architecture.md`. Nada inventado (Artigo IV).

### `@dnd-kit` é a lib canônica
A arquitetura trava `@dnd-kit/core` + `sortable` para reordenação acessível (o PRD também explicita). Adicionar essas deps (PRE-2). [Source: architecture.md § 3.1; prd.md Story 3.4 AC1]

### `reorderLinks({ ids })`
Input canônico `{ ids: string[] }` na ordem desejada; a action valida ownership (todos os ids do `user.id`) e faz **update em lote** de `position`. Nunca reordena links de outro usuário. [Source: architecture.md § 5.2; § 2.5 authz app-layer]

### Acessibilidade
Drag-and-drop **e** alternativa por teclado (↑/↓) com `aria-label` e foco correto — atende a11y sem exigir teste formal de screen reader. Área de maior atenção do epic (PRD §7 destaca dnd acessível). [Source: prd.md Story 3.4 AC3; §7 MVP Scope]

### Optimistic + latência
Reordenar optimistic no client; persistência assíncrona; alvo <200ms perceptível. Reconciliar se o server falhar. [Source: prd.md Story 3.4 AC5]

### Dependências
Depende de **3.3** (estende a lista de links). Wave 3 (isolada). A ordem reflete imediatamente na página pública (Story 3.5) via revalidação.

---

## Definition of Done

- [ ] Drag-and-drop (`@dnd-kit`) reordena optimistic; `reorderLinks` persiste em lote.
- [ ] Alternativa teclado (↑/↓) com aria-labels e foco corretos.
- [ ] `reorderLinks` valida ownership (não reordena links de outro).
- [ ] Integração com 20+ links verde; latência <200ms perceptível.
- [ ] `pnpm lint` + `pnpm test` verdes. `File List` preenchido. Sem push.

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** UI interativa (drag-and-drop) + Server Action de reorder.
- **Complexity:** **Medium** — `@dnd-kit`, update em lote, a11y por teclado, teste com volume.
- **Primary Agents:** `@dev`, `@qa`. **Supporting:** `@architect` (gate Wave 3), `@ux-design-expert` (a11y — opcional), `@devops` (push).
- **Focus Areas:** acessibilidade do drag-and-drop, update em lote de position, ownership no reorder.

---

## Project Structure Notes

- Estende `app/dashboard/links/`; `reorderLinks` em `lib/actions/links.ts`; componentes sortable em `components/`. [Source: architecture.md § 5.2, § 6.2]

---

## Dev Agent Record

### Agent Model Used
_(preenchido pelo @dev)_

### Debug Log References
_(preenchido pelo @dev)_

### Completion Notes
_(preenchido pelo @dev)_

### File List
_(preenchido pelo @dev)_

---

## QA Results

> Preenchido pelo `@qa` na Phase 4.

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-01 | Draft inicial (PRD § 6 Epic 3 + architecture.md § 3.1/§ 5.2). @dnd-kit + reorderLinks({ids}). | @sm (River) |
| 0.2 | 2026-07-01 | GO 9/10 — Status Draft→Ready. | @po (Pax) |

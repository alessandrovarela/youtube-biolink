---
id: "3.6"
epic: 3
title: "Tratamento de `@` e reserved routes (decisão de roteamento)"
status: Ready for Review
owner_persona: "Dev"
agent_owner: "@architect"
executor: "@architect"
quality_gate: "@qa"
quality_gate_tools: ["pnpm test:integration", "pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-07-01"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md § 6 — Epic 3, Story 3.6"
  - "docs/architecture.md § 2.5 (Reserved-list Routing)"
  - "docs/stories/2.3.story.md — lista de reservados"
  - "docs/stories/epics/epic-3/EPIC-3-EXECUTION.yaml — Wave 1"
depends_on: []
---

# Story 3.6 — Tratamento de `@` e reserved routes

## Status

**Ready for Review** — implementada pelo `@architect`. Aguarda QA Gate (`@qa`).

---

## Story

**Como** dev,
**eu quero** que o roteamento da página pública não conflite com rotas internas reservadas,
**para que** `/login`, `/dashboard`, `/signup`, etc. continuem funcionando.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 3, Story 3.6.

1. Estrutura de rotas escolhida garante ausência de ambiguidade. **Decisão já resolvida na arquitetura v0.3 (H1): `app/[username]/page.tsx` + reserved-list, SEM middleware rewrite no MVP.** Esta story **documenta e valida** essa decisão.
2. Nomes reservados (Story 2.3) não podem ser registrados como username.
3. Acessar `/dashboard` (e demais rotas internas) não conflita com a rota dinâmica `[username]` — rotas estáticas vencem por precedência do App Router.
4. Documentar decisão em `docs/architecture/routing.md`.

---

## Tasks / Subtasks

- [x] **T1. Documento de roteamento** (AC: 1, 4)
  - [x] Criar `docs/architecture/routing.md`: decisão `app/[username]` + reserved-list; por que **sem** middleware rewrite no MVP; precedência do App Router (estáticas > dinâmica); nota de que o `@` é display-only (path é `/username`); middleware edge fica no Epic 6 (Story 6.5).

- [x] **T2. Confirmar reserved-list ↔ registro de username** (AC: 2)
  - [x] Verificar que a lista de reservados da Story 2.3 (`validateUsername`) cobre os segmentos de rota internos (`login`, `signup`, `dashboard`, `auth`, `reset-password`, `health`, etc.) e bloqueia registro. **Cobertura completa** — todos os segmentos de topo de `app/` (`auth`, `dashboard`, `health`, `login`, `reset-password`, `signup`) já constam de `RESERVED_USERNAMES`.
  - [x] Se algum segmento de rota interno faltar na lista, adicioná-lo (extend-only). **N/A** — nada faltava; código de validação não foi alterado.

- [x] **T3. Teste de não-conflito** (AC: 3)
  - [x] Teste unitário `tests/unit/reserved-routes.test.ts`: para cada segmento interno (`login`, `signup`, `dashboard`, `auth`, `reset-password`, `health`, `api`), `validateUsername(segmento)` retorna `ok: false` (não registrável); format-válidos → `error: 'reserved'`; `reset-password` → `invalid_format` (bloqueio mais forte pela regex). A precedência estática > dinâmica é garantia do App Router, documentada em `routing.md § 2` em vez de testada via servidor.

- [x] **T4. Atualizar File List e Change Log.**

---

## Dev Notes

> **CRITICAL:** Decisão vem de `architecture.md`. Nada inventado (Artigo IV).

### Decisão canônica (Reserved-list Routing)
`app/[username]/page.tsx` cobre a página pública; rotas estáticas (`/login`, `/dashboard`, `/signup`) **vencem por precedência do App Router**; a reserved-list (Story 2.3) bloqueia registros conflitantes. **Não é necessário middleware rewrite** — o framework resolve. [Source: architecture.md § 2.5 Reserved-list Routing]

### `@` é display-only
O PRD usa `/@username` como rótulo de marca; o **path real** é `/[username]` (sem `@` na URL do App Router). O rewrite `/@username → /[username]` é stretch **opcional do Epic 6** (Story 6.5), não do MVP. Documentar esse ponto explicitamente em `routing.md`. [Source: architecture.md § 2.5; prd.md Story 6.5 escopo 5]

### Executor
Story de **decisão/documentação de arquitetura** → executor `@architect` (autoridade de roteamento). Baixo código (doc + validação da reserved-list + teste de precedência). [Source: agent-authority.md — @architect Design Authority]

### Dependências
Depende conceitualmente da reserved-list da **Story 2.3** (já entregue no Epic 2). Destrava a estrutura de rota da **Story 3.5**. Wave 1 (paralela com 3.1 e 3.2).

---

## Definition of Done

- [x] `docs/architecture/routing.md` documenta a decisão `[username]` + reserved-list + `@` display-only.
- [x] Reserved-list cobre todos os segmentos de rota internos e bloqueia registro.
- [x] Teste confirma que rotas internas não colidem com `[username]`.
- [x] `pnpm lint` verde. `File List` preenchido. Sem push.

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** Decisão de arquitetura + documentação + teste de roteamento.
- **Complexity:** **Low** — decisão já tomada na arquitetura; foco em formalizar e provar não-conflito.
- **Primary Agents:** `@architect`, `@qa`. **Supporting:** `@dev` (ajuste de reserved-list se necessário), `@devops` (push).
- **Focus Areas:** precedência estática > dinâmica, cobertura da reserved-list, clareza do `@` display-only.

---

## Project Structure Notes

- `docs/architecture/routing.md`; ajuste eventual em `lib/validation` (reserved-list da Story 2.3); teste em `tests/integration/`. [Source: architecture.md § 2.5, § 6.2]

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (1M context) via `@architect` (Aria) — modo YOLO (autônomo).

### Debug Log References
- `pnpm test:unit` → 34 passed (3 files). O teste novo `reserved-routes.test.ts` inicialmente falhou em `reset-password` (retorna `invalid_format`, não `reserved`, por causa do hífen barrado na regex antes da reserved-list); assertivas ajustadas para refletir a ordem real de checagem de `validateUsername` (No Invention — validação não foi reordenada).
- `pnpm typecheck` → OK. `pnpm lint` → OK.

### Completion Notes
- **Decisão formalizada, não inventada:** `routing.md` transcreve a decisão H1 já resolvida em `architecture.md § 2.5` (rota `app/[username]` + reserved-list, sem middleware rewrite no MVP; `@` display-only; middleware edge → Epic 6 / Story 6.5).
- **Cobertura da reserved-list já completa:** todos os segmentos de rota de topo em `app/` (`auth`, `dashboard`, `health`, `login`, `reset-password`, `signup`) já constavam de `RESERVED_USERNAMES`. **Nenhuma extensão foi necessária** — `lib/validation/username.ts` não foi modificado.
- **Precedência estática > dinâmica** é garantia do Next.js App Router: documentada em `routing.md § 2` em vez de testada via servidor (um teste de servidor apenas reconfirmaria comportamento interno do framework). O teste unitário cobre a metade que depende de código do projeto — a reserved-list.
- Segmentos aninhados (`dashboard/links`, `signup/check-email`, `auth/callback`, `reset-password/confirm`) não competem com o path de segmento único `/[username]`, logo não precisam estar na reserved-list.

### File List
- `docs/architecture/routing.md` (novo) — decisão canônica de roteamento.
- `tests/unit/reserved-routes.test.ts` (novo) — teste de cobertura reserved-list ↔ rotas internas.
- `docs/stories/3.6.story.md` (modificado) — status, checkboxes, records, change log.
- _Nota:_ `lib/validation/username.ts` **inspecionado, não modificado** (cobertura já completa).

---

## QA Results

**Gate: PASS** — @qa (Quinn), 2026-07-01 · commit `684c950`

| Checagem | Resultado |
|----------|-----------|
| 1. Code review | ✅ `routing.md` documenta `[username]` + reserved-list, precedência estática>dinâmica, `@` display-only, middleware→Epic 6. Sem alteração de código (cobertura já completa). |
| 2. Testes | ✅ `reserved-routes.test.ts` prova que todo segmento interno é não-registrável. |
| 3. ACs | ✅ AC1–4 atendidas. |
| 7. Docs | ✅ Decisão formalizada, rastreável a § 2.5 (No Invention). |

**Nota técnica (aprovada):** `reset-password` é barrado por `invalid_format` (hífen na regex) antes da reserved-list; o teste asserta `ok:false` para todos e o código específico por caso. Ordem de validação **não** foi reordenada — decisão correta (evita invenção). Bloqueio efetivo garantido.

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-01 | Draft inicial (PRD § 6 Epic 3 + architecture.md § 2.5). Executor @architect. | @sm (River) |
| 0.2 | 2026-07-01 | GO 9/10 — Status Draft→Ready. | @po (Pax) |
| 0.3 | 2026-07-01 | Implementado (routing.md + cobertura reserved-list + teste). Status → Ready for Review | @architect (Aria) |

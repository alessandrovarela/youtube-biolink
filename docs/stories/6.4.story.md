---
id: "6.4"
epic: 6
title: "Rate limiting (signup, login, reset, tracking)"
status: Done
owner_persona: "Dev"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pnpm typecheck", "pnpm lint", "pnpm test", "pnpm dev"]
created_by: "@sm (River)"
created_at: "2026-07-19"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md — FR21 (L66); NFR18 (L90); NFR19 (L92); NFR8 (L82); ACs deferidas das Stories 2.3/2.4/2.5 e 5.2 AC5 (L720)"
  - "docs/architecture/security-epic-6.md — ADR-001 § 3 (Decisão 2: Supabase-native; Upstash/Vercel KV refutados)"
  - "docs/stories/epics/epic-6/EPIC-6-EXECUTION.yaml — Story 6.4, decisão #2"
  - "docs/architecture/epic-6-data-access-inventory.md — § 6 (débito: click inflation não fechado pela RLS)"
depends_on: ["6.3"]
wave: 3
complexity: 5
---

# Story 6.4 — Rate limiting (signup, login, reset, tracking)

## Status

**InReview** — implementada pelo `@dev` (Dex). Migration aplicada em `development`,
suíte completa verde (287 testes) e o vetor de click inflation **fechado e provado
empiricamente** (ver Completion Notes). Aguarda o quality gate do `@qa`.

---

## Story

**Como** mantenedor do produto,
**eu quero** throttling nos endpoints de signup, login, reset de senha e tracking de clique,
**para que** o produto resista a força bruta de credenciais, enumeração e click inflation — sem adicionar dependência, serviço externo ou custo (FR21, NFR18, NFR8).

---

## Contexto do problema

FR21 e NFR18 foram **explicitamente deferidos** do MVP para o Epic 6, e as ACs de rate limiting das Stories 2.3/2.4/2.5 e 5.2 AC5 apontam para cá. Além disso, a Story 6.3 fechou o INSERT anônimo em `link_clicks` mas **não** fecha *click inflation*: um atacante ainda pode repetir chamadas à RPC `record_link_click` num link ativo legítimo. Esta story é a mitigação. [Source: inventory § 6 "Débitos que a RLS NÃO fecha"]

O problema técnico: Server Actions rodam em funções **efêmeras** na Vercel, sem memória compartilhada entre instâncias. Contadores exigem store externo.

**Decisão arquitetural (ADR-001 § 3, ratificada, não reabrir):** **Supabase-native** — tabela de contadores + função SQL `SECURITY DEFINER` com **janela deslizante por sub-buckets**. Upstash e Vercel KV foram **refutados**: adicionariam SDK, conta, credenciais e um terceiro ponto de falha, contra o princípio zero-dep e contra NFR8 (free tier). O Postgres do Supabase já está no caminho crítico dos quatro endpoints. A decisão é **reversível** — migrar para Upstash depois é trocar a implementação de **uma** função (`lib/rate-limit.ts`).

---

## Acceptance Criteria

> Toda AC rastreia PRD (FR21/NFR18/NFR19/NFR8) ou ADR-001 § 3 (Artigo IV).

### Bloco A — Infraestrutura de banco

1. **Tabela `public.rate_limit_counters`** (migration `{ts}_rate_limit.sql`): `bucket text not null`, `subject text not null`, `window_start timestamptz not null`, `hits int not null default 0`, `primary key (bucket, subject, window_start)` + índice em `(window_start)`.
   **`subject` é sempre um digest hex de 64 chars — nunca IP raw** (NFR19).
   [Source: ADR-001 § 3 "Desenho concreto"]

   > **Emenda 2026-07-27 (@po):** a cláusula "sempre 64-hex" vale para os **baldes de autenticação** (`signup`/`login`/`reset`, onde o `subject` é o hash SHA-256 do IP, 64-hex). O balde **`track_link`** usa o `link_id` (uuid) como `subject`, por decisão de desenho da **Emenda 1 do ADR** — o teto de rate limit do tracking foi movido para **dentro** da RPC `record_link_click`, que conhece **exclusivamente** o `link_id` e não deriva hash de IP. **Não há violação de NFR19:** um uuid de link não é PII e nenhum IP raw é persistido (ver AC17 e a seção **Reconciliação TD-5** abaixo). Emenda aplicada; TD-5 resolvido.

2. **Tabela puramente interna:** `alter table public.rate_limit_counters enable row level security;` **sem policy** (deny-all) **e** `revoke all on table public.rate_limit_counters from anon, authenticated;`. Só a função `SECURITY DEFINER` a enxerga.
   *Given* a migration aplicada, *when* um cliente com a anon key faz `GET`/`POST` em `/rest/v1/rate_limit_counters`, *then* erro/0 linhas.
   [Source: ADR-001 § 3]

3. **Função `public.check_rate_limit(p_bucket text, p_subject text, p_limit int, p_window_seconds int, p_bucket_seconds int default 60) returns boolean`**, `language plpgsql`, `security definer`, **`set search_path = public`**. Retorna `true` = permitido (e já contabilizado); `false` = estourado.
   Algoritmo (janela deslizante por sub-buckets): `pg_advisory_xact_lock` na chave (serializa check+increment sob concorrência) → soma `hits` dos buckets com `window_start` dentro da janela → se `>= p_limit` retorna `false` **sem incrementar** (não estende a punição) → senão faz upsert `on conflict (bucket, subject, window_start) do update set hits = hits + 1` e retorna `true`.
   [Source: ADR-001 § 3]

4. **Housekeeping oportunista:** em ~1% das chamadas (`random() < 0.01`), apaga registros com `window_start < now() - interval '24 hours'`. Sem cron, sem infra adicional (a janela mais longa é 1h — nada de valor se perde).
   [Source: ADR-001 § 3 e § 4]

5. **FAIL-OPEN explícito.** A função termina com `exception when others then return true;`. Um limiter quebrado **nunca** derruba o produto. Trade-off consciente de disponibilidade sobre throttle, comentado na própria migration.
   [Source: ADR-001 § 3 e § 4]

6. **Grants explícitos:** `revoke all on function public.check_rate_limit(text, text, int, int, int) from public;` + `grant execute ... to anon, authenticated;` + `notify pgrst, 'reload schema';`
   [Source: ADR-001 § 3]

### Bloco B — Helper de aplicação

7. **`lib/rate-limit.ts` com zero dependência nova** (`node:crypto` é built-in; `next/headers` já em uso). Expõe `checkRateLimit(bucket: 'signup' | 'login' | 'reset' | 'track', extraKey?: string): Promise<boolean>` — wrapper fino chamado **antes** de qualquer trabalho caro na action.
   [Source: ADR-001 § 3]

8. **IP obtido dos headers da Vercel, usado apenas em memória.** `x-forwarded-for` → **primeiro** elemento da lista (leftmost = cliente); fallback `x-real-ip`; fallback `'unknown'`. O IP **nunca** é gravado, logado ou enviado ao banco.
   [Source: ADR-001 § 3 · PRD NFR19]

9. **`RATE_LIMIT_PEPPER` — único env novo do epic.** `subjectHash` = `SHA-256` de `[pepper, ...parts].join(':')`. String aleatória, **server-only**, **NUNCA** `NEXT_PUBLIC_*`. Sem pepper o hash ainda funciona, mas fica sujeito a enumeração do espaço IPv4.
   *Given* o repositório, *when* se inspeciona `.env.example`, *then* `RATE_LIMIT_PEPPER` está listado com placeholder e comentário de que é server-only e nunca `NEXT_PUBLIC_*`.
   **⚠️ Gate de merge:** `@devops` (exclusivo) provisiona `RATE_LIMIT_PEPPER` nos ambientes Vercel (production **e** preview) **ANTES** do merge desta story. Sinalizar no handoff.
   [Source: ADR-001 § 3 · EPIC-6-EXECUTION.yaml decisão #3 revisada (PRE-3 cancelado; este env o substitui) · PRD NFR10]

10. **Nenhuma secret privilegiada introduzida.** `RATE_LIMIT_PEPPER` **não é** credencial de acesso: seu vazamento permite correlacionar hashes de IP; ele **não** dá acesso a nada. Contraste didático com a service role key (que entregaria o banco inteiro) — registrar nas Dev Notes da migration/story.
    [Source: ADR-001 § 3]

### Bloco C — Targets do NFR18 (fixos — não inventar outros)

11. **Os quatro buckets aplicados com os targets exatos:**

| Bucket | Limite | Janela | `p_bucket_seconds` | `subject` | Ponto de aplicação |
|---|---|---|---|---|---|
| `signup` | 5 | 3600s | 60 | `hash(ip)` | Server Action de signup |
| `login` | 10 | 900s | 60 | `hash(ip)` | Server Action de login |
| `reset` | 3 | 3600s | 60 | `hash(ip)` | Server Action de reset de senha |
| `track` | 60 | 60s | 10 | `hash(ip + ':' + linkId)` | `trackLinkClick` |

*Given* mais de N requisições na janela, *when* a N+1 chega, *then* `checkRateLimit` retorna `false` e a action curto-circuita antes do trabalho caro.
[Source: PRD NFR18 · ADR-001 § 3 tabela de targets]

### Bloco D — Comportamento no estouro

12. **signup / login / reset → erro genérico e amigável**, no mesmo formato `FormState` já usado em `lib/actions/auth.ts` (ex.: *"Muitas tentativas. Aguarde alguns minutos e tente novamente."*).
    *Given* o limite estourado no login, *then* a mensagem **não** revela o mecanismo (sem "rate limit", sem contadores, sem `Retry-After` detalhado) **e não permite enumerar contas** — a mensagem é idêntica exista ou não a conta.
    [Source: EPIC-6-EXECUTION.yaml notes 6.4 · ADR-001 § 3]

13. **tracking → no-op silencioso.** `trackLinkClick` retorna erro tipado como sempre e **a navegação do visitante nunca é bloqueada** (contrato das Stories 5.2/5.3 preservado). Nenhuma mensagem na UI.
    [Source: PRD Story 5.2 AC6 · ADR-001 § 3]

14. **Falha do próprio limiter → fail-open** (AC5) — nada é bloqueado e nenhuma exceção sobe para a UI.
    [Source: ADR-001 § 3]

### Bloco E — Testes

15. **Testes unitários da lógica de janela deslizante:** dentro do limite passa; no limite exato bloqueia; após a janela expirar volta a passar; requisições estouradas **não** incrementam (não estendem a punição); `clientIp` extrai o leftmost de `x-forwarded-for`, cai para `x-real-ip` e depois `'unknown'`; `subjectHash` é determinístico e muda com o pepper.
    [Source: EPIC-6-EXECUTION.yaml notes 6.4]

16. **Testes de integração dos 4 endpoints** contra `development`: cada bucket atinge o target e bloqueia; a mensagem de auth é genérica; o tracking estourado não lança nem bloqueia; fail-open verificado.
    [Source: EPIC-6-EXECUTION.yaml notes 6.4 · PRD NFR15]

17. **Sem IP raw persistido em lugar nenhum.**
    *Given* a suíte + inspeção do schema, *when* se consulta `rate_limit_counters`, *then* `subject` casa com `^[0-9a-f]{64}$` em todas as linhas; *and* `link_clicks` continua sem coluna de IP.
    [Source: PRD NFR19 · ADR-001 § 3]

    > **Emenda 2026-07-27 (@po):** o casamento `^[0-9a-f]{64}$` "em todas as linhas" aplica-se às linhas dos **baldes de autenticação** (`signup`/`login`/`reset`). O balde **`track_link`** usa o `link_id` (uuid) como `subject` (Emenda 1 do ADR — teto movido para dentro de `record_link_click`). Isso **não viola o NFR19**: o requisito real da AC17 é "sem IP raw persistido", que permanece **satisfeito** (uuid de link não é PII). Ver a Emenda em AC1 e a seção **Reconciliação TD-5**.

18. **Zero dependência nova.** `package.json` sem novas entradas em `dependencies`.
    [Source: ADR-001 § 3 · EPIC-6-EXECUTION.yaml decisão #2]

19. **Migration aplicada em `development` via `supabase db push`**; produção via CD `migrate-production` no merge.
    [Source: PRD FR22 · EPIC-6-EXECUTION.yaml decisão #5d]

---

## Escopo

### IN
- Migration `{ts}_rate_limit.sql` (tabela + RLS deny-all + revoke + função + grants).
- `lib/rate-limit.ts` (helper de IP, hash com pepper, wrapper `checkRateLimit`).
- Integração nos 4 pontos: signup, login, reset (`lib/actions/auth.ts`) e `trackLinkClick` (`lib/actions/track-click.ts`).
- `.env.example` com `RATE_LIMIT_PEPPER`.
- Testes unitários + integração.

### OUT (Artigo IV — não inventar controles fora do PRD)
- **Captcha, WAF, bloqueio por conta/e-mail, banimento persistente, `pg_cron`, fingerprinting de dispositivo** — explicitamente não-escopo do ADR-001 § 4 e da decisão #6 do epic.
- Rate limit por conta/usuário (o NFR18 contratou **por IP**; "por conta quando aplicável" do FR21 não tem target definido → não implementar sem contrato).
- Upstash / Vercel KV / Redis (refutados pelo ADR).
- Cabeçalhos `Retry-After` / `X-RateLimit-*` (vazariam o mecanismo — contra o AC12).
- Middleware/CSP (Story 6.5).

---

## Tasks / Subtasks

- [x] **T1. Migration `{ts}_rate_limit.sql`** (AC: 1–6)
  - [x] Tabela + PK composta + índice em `window_start`.
  - [x] `enable row level security` sem policy + `revoke all ... from anon, authenticated`.
  - [x] `check_rate_limit(...)` com `security definer`, `set search_path = public`, advisory lock, soma da janela, upsert, housekeeping ~1%, `exception when others then return true`.
  - [x] `revoke all on function ... from public` + `grant execute ... to anon, authenticated` + `notify pgrst`.
  - [x] Comentário documentando o fail-open como trade-off consciente.

- [x] **T2. Aplicar em `development`** (AC: 19)

- [x] **T3. `lib/rate-limit.ts`** (AC: 7, 8, 9, 10)
  - [x] `clientIp()` (leftmost de `x-forwarded-for` → `x-real-ip` → `'unknown'`), uso **efêmero**.
  - [x] `subjectHash(...parts)` com `RATE_LIMIT_PEPPER` via `node:crypto`.
  - [x] `checkRateLimit(bucket, extraKey?)` → `supabase.rpc('check_rate_limit', …)`.

- [x] **T4. Integrar nos 4 endpoints** (AC: 11, 12, 13)
  - [x] `lib/actions/auth.ts`: signup / login / reset — chamada **antes** de qualquer trabalho caro; estouro → `FormState` com mensagem genérica.
  - [x] `lib/actions/track-click.ts`: bucket `track` com `hash(ip + ':' + linkId)`; estouro → no-op silencioso, nunca lança.

- [x] **T5. `.env.example` + handoff a `@devops`** (AC: 9)
  - [x] Adicionar `RATE_LIMIT_PEPPER=` com comentário (server-only, nunca `NEXT_PUBLIC_*`).
  - [x] Registrar no Completion Notes: **`@devops` deve provisionar em production e preview ANTES do merge.**

- [x] **T6. Testes** (AC: 15, 16, 17, 18)
  - [x] Unit: janela deslizante, não-incremento no estouro, `clientIp`, `subjectHash`.
  - [x] Integração: os 4 buckets; mensagem genérica; tracking silencioso; fail-open.
  - [x] Assertion de `subject` = 64 hex.

- [x] **T7. Smoke manual + docs**
  - [x] `pnpm dev`: estourar login e ver a mensagem genérica; clicar rápido num link e confirmar que a navegação nunca trava.
  - [x] Atualizar `docs/architecture/ER.md` (nova tabela), File List e Change Log.

---

## Dev Notes

### Por que janela deslizante por sub-buckets
A janela é fatiada em sub-buckets fixos (padrão 60s); o consumo é a **soma dos buckets cujo início cai dentro da janela**. Diferente do contador fixo, **não permite o pico de 2× na virada**; diferente de "uma linha por evento", o custo de linhas é limitado (`janela / bucket` — ex.: 60 linhas por chave numa janela de 1h). Simples de implementar, de testar e de explicar. [Source: ADR-001 § 3]

### Por que `pg_advisory_xact_lock`
Serializa por chave: o check e o increment viram **uma operação atômica** sob concorrência, sem read-modify-write frágil. É o ganho de correr no Postgres em vez de num KV eventualmente consistente. [Source: ADR-001 § 3]

### Por que não incrementar quando já estourou
Se cada requisição bloqueada também incrementasse, um cliente em loop **estenderia a própria punição indefinidamente** — a janela nunca esvaziaria. O ADR fixa `return false` antes do upsert. [Source: ADR-001 § 3]

### Fail-open é uma escolha, não um bug
`exception when others then return true`: um limiter quebrado não bloqueia ninguém. Disponibilidade acima de throttle num produto didático — trade-off explícito do ADR § 3/§ 4. Documentar na migration para que o `@qa` não o trate como defeito.

### Limitações aceitas (registrar, não corrigir)
- **NAT/CGNAT** compartilham IP → o limite por IP pode afetar usuários coabitantes. É o que o NFR18 contratou; nada além disso (Artigo IV).
- **`subject = 'unknown'`** agrupa requests sem header num balde único que pode saturar e barrar terceiros. Na Vercel o header sempre existe; é só uma guarda.
- **Custo de escrita no free tier** (NFR8): mitigado pelo bucket de 60s (agrega N eventos em 1 linha) e pelo housekeeping de 24h.
- **Round-trip extra** em signup/login/reset/tracking. Aceitável no volume alvo; **medir no gate se o tracking regredir** (há `clicks-benchmark.test.ts`).
[Source: ADR-001 § 4]

### Dependências
Depende da **Story 6.3** (toca os mesmos endpoints que foram refatorados; `trackLinkClick` já usa a RPC). PRE-1 (ADR-001) satisfeito. Zero dependências npm novas. **Um env novo: `RATE_LIMIT_PEPPER`** (não privilegiado).

---

## Riscos

| # | Risco | Severidade | Mitigação |
|---|---|---|---|
| R1 | `RATE_LIMIT_PEPPER` ausente na Vercel no momento do merge → hashes sem pepper em prod | **Alta** | AC9: gate de merge com `@devops`; código tolera ausência (`?? ''`) mas o handoff sinaliza explicitamente |
| R2 | Mensagem de estouro no login permite **enumerar contas** | **Alta** | AC12: mensagem idêntica exista ou não a conta; verificado no gate |
| R3 | Rate limit no tracking bloqueia a navegação do visitante | **Alta** | AC13: no-op silencioso; teste dedicado; contrato 5.2/5.3 |
| R4 | Estouro incrementando o contador estende a punição para sempre | Média | AC3: `return false` antes do upsert; teste unitário dedicado |
| R5 | `check_rate_limit` mal configurada (`SECURITY DEFINER` sem `search_path`/grants) | Média | AC3/AC6 + revisão de DDL |
| R6 | Round-trip extra degrada o tracking | Baixa | Medir com `clicks-benchmark.test.ts` no gate |
| R7 | Crescimento descontrolado de `rate_limit_counters` | Baixa | Housekeeping oportunista (AC4) + bucket de 60s |

---

## Débitos ACEITOS (registrados, não corrigidos)

> Rate limiting limita **volume**, nunca **autenticidade**. Nenhum ajuste de limite torna
> um clique forjado distinguível de um real — isso exigiria prova de humanidade por
> visitante (captcha), **fora do PRD** (Artigo IV). O vetor está fechado como *bypass*
> (nenhum caminho escapa do teto) e *bounded* como abuso — não eliminado.

| # | Débito | Severidade | Justificativa de aceite |
|---|---|---|---|
| D1 | **Atacante direto ainda grava até 60 cliques/min/link** (~86k/dia) chamando a RPC pelo PostgREST com a anon key pública, sem passar pelo Next.js. | Média | É o piso teórico de qualquer desenho sem prova de humanidade. A camada 3 do ADR (descartada pelo `@pm`) **não** melhoraria este número — ela elevaria o teto do chamador *confiável* para 600, deixando o do atacante igual em 60. |
| D2 | **Link legitimamente viral acima de 60 cliques/min subnotifica.** Cliques acima do teto não são enfileirados nem repostos: a analytics vira um **piso**, não um total. | Média | Consequência direta de D1 e do descarte da camada 3, que existia justamente para comprar essa folga (600/min). Trocamos precisão em cauda extrema por não semear um segredo à mão fora do versionamento em 2 ambientes. Proteger o free tier (NFR8) pesa mais neste produto. |
| D3 | **Ataque distribuído evade a camada 2 por construção** (muitos IPs, cada um sob 60/min). | Baixa | É exatamente por isso que a camada 1 **não** é chaveada por IP. A camada 1 continua limitando o total por link. |
| D4 | **A camada 1 herda o fail-open de `check_rate_limit`**: erro interno do limiter libera as escritas. | Baixa | Consistência com signup/login, onde disponibilidade pesa mais. Um limiter quebrado degrada para o comportamento pós-6.3 — ainda melhor que o do MVP. |
| D5 | **Contenção do advisory lock por link:** todo clique no mesmo link serializa em `track_link:<uuid>`. | Baixa | Lock de escopo de transação, dura microssegundos. É o ponto quente previsível se um link viralizar — e o teto de 60/min já limita a fila. |
| D6 | **NAT/CGNAT compartilham IP** → o limite por IP pode afetar usuários coabitantes. | Baixa | É o que o NFR18 contratou. Nada além (Artigo IV). |
| D7 | **`subject = 'unknown'`** agrupa requests sem header de IP num balde único que pode saturar. | Baixa | Na Vercel o header sempre existe; o caminho é só uma guarda. |
| D8 | **Contadores órfãos de outros arquivos de teste** permanecem em `rate_limit_counters` (links já deletados, `hits = 1`). | Info | Cardinalidade baixa e o housekeeping de 24h os remove. Não justifica limpeza cross-arquivo nos testes. |
| D9 | `trackLinkClick` retorna `'Link não encontrado ou inativo'` também quando o **teto do link** estourou. | Info | String preservada de propósito (equivalência comportamental verificada pelo gate da Wave 2). A mensagem **nunca é renderizada** — o tracking é fire-and-forget. O significado ampliado está comentado no código. |

---

## Definition of Done

- [x] Migration aplicada em `development`: tabela deny-all + `check_rate_limit` com `search_path`, advisory lock, housekeeping e fail-open.
- [x] `lib/rate-limit.ts` com IP efêmero + hash com pepper; **zero dependência nova**.
- [x] Os 4 targets do NFR18 aplicados (5/h, 10/15min, 3/h, 60/min/linkId) nos 4 pontos.
- [x] Estouro em auth = mensagem genérica que não vaza o mecanismo nem enumera contas; estouro em tracking = no-op silencioso com navegação intacta.
- [x] Nenhum IP raw persistido; `subject` sempre 64 hex (NFR19).
- [x] `RATE_LIMIT_PEPPER` em `.env.example` (server-only, nunca `NEXT_PUBLIC_*`) **e** sinalizado a `@devops` para provisionamento em production/preview **antes do merge**.
- [x] Testes unitários da janela + integração dos 4 endpoints verdes.
- [x] `pnpm typecheck` / `pnpm lint` / `pnpm test` (suíte completa) verdes **e** `pnpm dev` sobe sem erro.
- [x] `docs/architecture/ER.md`, File List e Change Log atualizados. Sem push (delegado a `@devops`).

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** Backend/segurança (DDL `SECURITY DEFINER` + helper TS + integração em 4 Server Actions).
- **Complexity:** **5** (alta) — algoritmo de janela deslizante, concorrência, 4 pontos de integração, env novo com gate de deploy.
- **Primary Agents:** `@dev` (executor), `@qa` (quality gate). **Supporting:** `@data-engineer` (revisão de DDL), `@devops` (env na Vercel + push).
- **Focus Areas:** janela deslizante, atomicidade com advisory lock, privacidade por hash com pepper (NFR19), fail-open, não-enumeração de contas.

---

## Project Structure Notes

- `supabase/migrations/{ts}_rate_limit.sql`
- `lib/rate-limit.ts` (novo)
- `lib/actions/auth.ts`, `lib/actions/track-click.ts`
- `tests/unit/rate-limit.test.ts` (novo), `tests/integration/rate-limit.test.ts` (novo)
- `.env.example`, `docs/architecture/ER.md`

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (1M context) — `@dev` (Dex)

### Debug Log References
- `supabase db push --linked` → `20260719190000_rate_limit.sql` aplicada em `development` (`rjsvkwjgsctqsswrospx`).
- Verificação de catálogo via Management API (`/v1/projects/{ref}/database/query`).
- Probe de ataque com `curl` direto ao PostgREST (números abaixo).

---

### ⚠️ DESVIO DO ADR: a Emenda 1 foi implementada PARCIALMENTE, por arbitragem do `@pm`

O § 6 do ADR-001 propôs **três** camadas. O `@pm` **descartou a camada 3** (app-proof).
**NÃO foram implementados:** `p_app_proof`, `private.app_secrets`, o env
`TRACKING_APP_PROOF`, o teto diferenciado de 600 para chamador confiável e o
`drop function` da assinatura antiga.

**Razão registrada:** o resultado de *segurança* é idêntico com ou sem a camada 3 — o
atacante direto grava 60/min/link nos dois desenhos. Ela só compraria folga para um link
legitimamente viral, ao custo de um segredo semeado à mão fora do versionamento em dois
ambientes (drift operacional garantido). Simplicidade didática prevalece.

**Consequências de desenho:**
1. Teto **único** de 60/min por link, para todos os chamadores.
2. A assinatura de `record_link_click` **permanece `(uuid, text)`** → `create or replace`
   substitui o corpo e **não** cria sobrecarga, então o `drop function` do § 6.4(b) do
   ADR deixa de ser necessário (ele só existia para matar a assinatura órfã que a v2 de
   3 argumentos teria deixado publicada, `grant execute to anon` e **sem teto**).
3. **Armadilha verificada explicitamente** — ver abaixo.

### ✅ Confirmação: existe EXATAMENTE UMA `record_link_click` publicada

Leitura direta do catálogo de `development` após a migration:

```
oid    | proname            | args                                      | prosecdef | proconfig            | proacl
41951  | record_link_click  | p_link_id uuid, p_user_agent_short text   | true      | {search_path=public} | postgres=X | anon=X | authenticated=X | service_role=X
```

**Uma linha, uma assinatura.** Nenhuma sobrecarga sem teto ficou publicada. A ausência da
entrada `=X/postgres` (PUBLIC) no `proacl` confirma que o `revoke all ... from public`
pegou. Idem para `check_rate_limit` (uma assinatura, `prosecdef`, `search_path` fixo).
Trava de regressão automatizada: `tests/integration/rate-limit.test.ts` assere via
OpenAPI do PostgREST que a RPC publica **exatamente** `{p_link_id, p_user_agent_short}` —
um parâmetro a mais denunciaria uma sobrecarga.

### 🔴 Prova empírica: o ataque direto do gate da Wave 2 agora é BARRADO

Reprodução do ataque do `@qa`, contra o **mesmo link ativo real**
(`a22e0cf2-62f8-4f08-8372-4570b208015d`), com `curl` direto em
`POST /rest/v1/rpc/record_link_click` usando a anon key crua — **sem tocar no Next.js**:

| Medida | Valor |
|---|---|
| Contagem de cliques ANTES | **1** |
| Chamadas curl diretas disparadas | **70** |
| Respostas `true` (gravaram) | **60** |
| Respostas `false` (barradas) | **10** |
| Primeira recusa | chamada **#61** |
| Contagem de cliques DEPOIS | **61** (= 1 + 60) |
| Linhas da probe gravadas | **60** (não 70) |

Sub-buckets deixados pelo ataque (`bucket = track_link`, `subject = <link_id>`):
`18:02:40 → 1 hit`, `18:02:50 → 51 hits`, `18:03:00 → 8 hits`. **Soma = 60, não 70** —
prova de quebra de que as 10 requisições estouradas **não incrementaram** o contador.

**Antes desta story as 70 teriam sido gravadas** (o gate provou com 20/20). O vetor está
fechado como bypass: nenhum caminho escapa do teto.

**Estado restaurado:** as 60 linhas da probe foram removidas e os contadores zerados;
a contagem do link voltou a **1**. Verificado após o cleanup.

### Gates executados

| Gate | Resultado |
|---|---|
| `pnpm typecheck` | **PASS** (exit 0, sem saída) |
| `pnpm lint` | **PASS** (exit 0, sem saída) |
| `pnpm test` (suíte completa) | **PASS** — 40 arquivos, **287 testes** passed, 1 skipped |
| `pnpm dev` | **PASS** — `Ready in 170ms`, `/login` 200, `/signup` 200, `/` 307, zero erros no log |

**Baseline 233 → 287 (+54 testes).** Progressão do epic: 192 (Epic 5) → 219 (Wave 1) →
233 (Wave 2) → **287 (Wave 3)**.

### Dois testes pré-existentes tiveram a EXPECTATIVA atualizada (não mascarados)

`revoke select on link_clicks from anon` (concern #2) mudou o comportamento observável do
SELECT anônimo de **`200 []`** (RLS filtrando em silêncio) para **`42501 permission
denied`** (privilégio ausente). É o resultado **mais forte**, e era exatamente o objetivo
do concern. Ajustados para asserir a negação por privilégio:
- `tests/integration/link-clicks-rls.test.ts` — "SELECT anônimo em link_clicks é NEGADO por PRIVILÉGIO (duas camadas)"
- `tests/integration/link-clicks.test.ts` — bloco final do teste de isolamento

Nenhuma asserção foi enfraquecida: as duas passaram de `expect(error).toBeNull()` +
`toHaveLength(0)` para `expect(error).not.toBeNull()` + `permission denied` + `data === null`.

### Onde a janela deslizante é testada (e por que não em unit)

A janela vive **inteiramente na função SQL**. Reimplementá-la em TS para "testar em unit"
seria **tautológico** — validaria a cópia, não o código que roda. Ela é exercida contra o
**banco real** em `tests/integration/rate-limit.test.ts`: dentro do limite passa, no
limite exato bloqueia, **virada de janela** (janela de 2s com sub-buckets de 1s, com
espera real) e o caso **"já estourou não incrementa"** (10 rejeições seguidas, contador
imóvel em 3). `tests/unit/rate-limit.test.ts` cobre o que é TS: `clientIp`, `subjectHash`,
targets do NFR18 e fail-open.

### ⚠️ HANDOFF PARA `@devops` — GATE DE MERGE

**`RATE_LIMIT_PEPPER` deve ser provisionado nos ambientes Vercel `production` E `preview`
ANTES do merge desta story.** Gerar com `openssl rand -hex 32`.

- **Server-only. NUNCA prefixar com `NEXT_PUBLIC_`** — isso o mandaria ao bundle do
  browser e anularia o propósito.
- **Não é credencial privilegiada:** seu vazamento permite *correlacionar* hashes de IP e
  nada mais. Contraste didático com a service role key, que entregaria o banco inteiro.
- **Comportamento se ausente (decidido e documentado):** o app **não quebra** e o rate
  limiting **continua ativo** — o hash é calculado com pepper vazio. O servidor emite
  **um** aviso no log (`lib/rate-limit.ts`), uma única vez por processo, para que a
  ausência não passe silenciosa. Degrada a *privacidade* do hash, nunca a disponibilidade.

Demais ações de `@devops`: push/PR (exclusivo) e a verificação pós-deploy já pendente dos
concerns #3 (Wave 2) e #2 (Wave 1).

### Completion Notes

- Camada 1 (teto na RPC, 60/min/link) + camada 2 (Server Action, 60/min por
  `hash(ip+linkId)`) + os 3 endpoints de auth (5/3600s, 10/900s, 3/3600s por `hash(ip)`).
- Migration **atômica**: tabela, índice, `enable RLS`, revokes, `check_rate_limit`,
  grants, teto na `record_link_click` e os revokes do concern #2 no **mesmo arquivo**.
- **Zero dependência nova** — `node:crypto` é built-in; `package.json` intocado.
- **App-layer preservada (NFR3):** nada foi removido; o rate limiting **somou**.
- **NFR19 verificado no dado, não só no código:** consulta ao schema confirma que
  `rate_limit_counters` não tem coluna de IP e que **0 linhas** de bucket de app têm
  `subject` fora de `^[0-9a-f]{64}$`.
- Produção **não** foi tocada (regra 3): a migration vai por CD no merge.

#### Correção pós-gate da Wave 3 (issues #1 HIGH, #2 MEDIUM, #3 LOW) — v1.1

**O defeito.** O gate provou que `check_rate_limit` era `grant execute to anon` com
`p_bucket`, `p_subject`, `p_limit`, `p_window_seconds` e `p_bucket_seconds` **inteiramente
controlados pelo chamador** — o limiter virou uma **primitiva de escrita remota**. Balanço
honesto do @qa: a story limitou a inflação de cliques e **abriu a supressão**. Corrigido.

**O desenho.** Separar **capacidade** de **política**:

| Função | Papel | Quem chama |
|--------|-------|-----------|
| `check_rate_limit(text,text,int,int,int)` | mecanismo (janela deslizante) | **INTERNA** — `anon`/`authenticated` perderam o `execute` |
| `check_app_rate_limit(text,text)` | política (allowlist + limites do NFR18) | pública, mas o chamador só escolhe **quem ele é** |

O wrapper (a) valida `p_bucket` contra a allowlist `signup|login|reset|track` — `track_link`
**fora**, que é o que fecha a supressão de analytics; (b) tem limite/janela/sub-bucket
**hardcoded** (o chamador não os escolhe); (c) exige `p_subject ~ '^[0-9a-f]{64}$'`,
transformando o NFR19 em invariante do **banco** e não só promessa do app.

**Por que não quebra a camada 1.** `record_link_click` é `SECURITY DEFINER`: seu corpo roda
como o **owner**, que mantém o `execute`. Confirmado empiricamente após aplicar a migration
(61 chamadas diretas ao PostgREST: 60 aceitas, a 61ª barrada).

**Prova de fechamento (probe própria, dev, antes → depois):**

| Abuso | Antes | Depois |
|-------|-------|--------|
| (a) supressão de analytics | 60 chamadas anônimas esgotaram o balde de link alheio; clique legítimo → `false` | `42501 permission denied`; clique legítimo → `true` |
| (b) lockout de auth | 10 chamadas anônimas com subject forjado; a 11ª (login real) → `false` | `42501`; e com o pepper obrigatório o subject deixa de ser computável |
| (c) bucket arbitrário | `bucket='lixo'` aceito e persistido (1 linha) | `22023 bucket lixo nao permitido`; **0 linhas** |
| não-regressão | teto 60/min/link, 1ª recusa na #61 | **idêntico** — 1ª recusa na #61, 60 cliques gravados |

**Issue #2 — `RATE_LIMIT_PEPPER` passa a ser OBRIGATÓRIO.** A classificação anterior
(degradação de *privacidade*) estava subestimada: com pepper vazio o subject de auth é
`sha256(':'+ip)`, público, e **10 requisições anônimas negavam login a um IP arbitrário**.
O pepper é o único controle que fecha o vetor (b) pelo caminho que **continua** aberto (o
wrapper), porque o subject precisa ser derivado no app — só ele enxerga o IP do visitante.
Comportamento novo: em `production` `lib/rate-limit.ts` **lança na carga do módulo** (o app
não sobe); fora dela, fallback fixo documentado + aviso único. Definido em `.env.local`
(development saiu do modo degradado) e o texto do `.env.example` foi reescrito.

**Issue #3 — fail-open agora é total.** As inicializações saíram do `DECLARE` (fora do
handler) para dentro do `begin`. Verificado: `p_bucket_seconds=0`, que antes propagava
`22012 division by zero` (HTTP 400), agora retorna `true`. O comentário didático voltou a
descrever o código com exatidão — o gate pediu corrigir o código, não a afirmação.

**Auditoria pedida (outras `SECURITY DEFINER` com grant a `anon` e parâmetros livres):**
o OpenAPI do PostgREST publica **exatamente duas** RPCs — `check_app_rate_limit` (nova,
estreita) e `record_link_click` (`p_link_id` tipado `uuid` e validado contra `links`;
`p_user_agent_short` só é truncado e gravado). `handle_new_user` é função de **trigger**,
não alcançável via PostgREST. **Nenhuma outra ocorrência do mesmo erro.**

**O que permanece descoberto (para o @qa / @architect):**
- **Gasto do próprio balde continua possível para quem conhecer o pepper** — mitigado, não
  eliminado. Fechá-lo por completo exigiria o IP autoritativo dentro do banco, que a
  Emenda 1 já refutou.
- **`service_role` mantém `execute` em `check_rate_limit`** — é o esperado (bypassa tudo
  por definição) e o único consumidor é a suíte de integração.
- **Issue #4 (AC1/AC17 vs. bucket `track_link`)** — correção de **texto de AC**, de @po/@sm.
  Não tocada aqui: `@dev` não edita AC.
- **Smoke pela UI real** (issue #7) — a Server Action de login não pôde ser exercitada por
  `curl` (a codificação RSC do Next rejeita o POST cru). Coberto por
  `tests/integration/rate-limit-app-path.test.ts`, que roda o helper de produção contra o
  banco real, mas um estouro manual pelo navegador continua recomendado antes do merge.

#### Correção pós-gate da Wave 4 (issue #1 MEDIUM) — v1.2

**O defeito, e ele era meu.** A v1.1 acima afirma, com estas palavras, que em produção
`lib/rate-limit.ts` "**lança na carga do módulo (o app não sobe)**". O gate da Wave 4
**testou** e a afirmação era **FALSA**. Com `RATE_LIMIT_PEPPER` removido e
`NODE_ENV=production`:

| Passo | Resultado observado |
|-------|--------------------|
| `pnpm build` | **EXIT 0** — o build passa |
| `pnpm start` | sobe limpo, sem erro no log |
| `GET /`, `/login`, `/signup`, `/health`, `/[username]` | 307/200/200/200/200 |
| erro do pepper no log após tudo isso | **0 ocorrências** |
| POST da Server Action de login REAL (header `Next-Action`) | **HTTP 500** — só aqui o erro aparece |

**A causa.** O Next carrega módulos de Server Action **sob demanda**. Um `throw` de topo
de módulo não roda no boot se o módulo não é carregado no boot. Armadilha geral de
validação de configuração em Next — vale registrar como lição, não como caso isolado.

**Por que era o pior posicionamento possível.** Tarde demais para impedir o deploy ruim,
cedo demais para ser inofensivo. Um deploy mal provisionado ficaria com **todos os sinais
verdes** — build, deploy, homepage, página pública, `/health` — enquanto login, signup,
reset **e** tracking devolvem 500. Monitoração de disponibilidade de página (o padrão) não
veria nada. Quem descobriria seria o usuário tentando entrar. Blast radius:
`lib/actions/auth.ts` inteiro mais `lib/actions/track-click.ts`.

**A correção — mover o gate para onde ele barra o DEPLOY.** `assertProductionEnv()` em
`next.config.ts`, avaliado **antes de qualquer compilação**, tanto em `next build` quanto
em `next start`:

- **Falha o build** → o artefato ruim nunca é gerado, logo nunca embarca. Na Vercel,
  build vermelho = sem promoção. É o único ponto que efetivamente **barra o deploy**.
- **Falha o boot** de `next start` → o self-hosted também quebra alto, e agora "o app não
  sobe" é verdadeiro **de fato**, e não só de intenção.

Reforçado por um passo **Preflight — required production env** em
`.github/workflows/ci.yml` (mensagem crua e legível, para que um build vermelho por env
ausente não exija arqueologia de stack trace) e pelo `env:` do passo `Build`. Os dois são
redundantes de propósito: se um for removido, o outro segura.

O `throw` de `lib/rate-limit.ts` **permanece** como **backstop** que em operação normal
nunca dispara — defense-in-depth aplicado a configuração, o mesmo princípio que o Epic 6
aplica a acesso.

**Prova empírica desta correção:**

```
$ RATE_LIMIT_PEPPER= pnpm build
⨯ Failed to load next.config.ts
Error:
[env-gate] BUILD DE PRODUÇÃO ABORTADO — env obrigatório ausente.

  ✗ RATE_LIMIT_PEPPER
      por quê: sem ele o subject dos buckets de auth é sha256(":"+ip) — computável
               por qualquer um — e um atacante nega login a IPs arbitrários (lockout).
      como: openssl rand -hex 32 · server-only, NUNCA NEXT_PUBLIC_* · valor distinto por ambiente
EXIT SEM ENV = 1

$ pnpm build          # com RATE_LIMIT_PEPPER de .env.local
✓ Compiled successfully
EXIT COM ENV = 0
```

**Development permanece funcional** (requisito explícito): `NODE_ENV=development` não
passa pelo gate. Verificado — `RATE_LIMIT_PEPPER= pnpm dev` → `✓ Ready in 121ms`,
`/login` → 200, com o aviso único do fallback documentado.

**Comentários corrigidos.** `lib/rate-limit.ts` deixou de afirmar "o app não sobe / quebra
no boot" e passou a descrever o comportamento **real** do `throw` (dispara na primeira
Server Action, com 500), por que isso o torna um backstop e não o gate principal, e onde
o gate de verdade mora. Num arquivo cujo propósito é didático, um comentário que descreve
comportamento inexistente ensina a coisa errada — o gate estava certo em cobrar.

### File List

**Criados**
- `supabase/migrations/20260719190000_rate_limit.sql`
- `lib/rate-limit.ts`
- `tests/unit/rate-limit.test.ts`
- `tests/unit/auth-rate-limit.test.ts`
- `tests/integration/rate-limit.test.ts`
- `supabase/migrations/20260719200000_rate_limit_hardening.sql` — **correção do gate da Wave 3** (issues #1/#3): `revoke execute` de `anon` em `check_rate_limit`, wrapper `check_app_rate_limit` (allowlist + limites hardcoded + subject 64-hex) e fail-open cobrindo o `DECLARE`
- `tests/integration/rate-limit-app-path.test.ts` — o caminho REAL do app (`checkRateLimit` de produção) contra o banco de development: os 3 buckets de auth barrando no teto do NFR18 e o vetor (b) fechado

**Modificados**
- `lib/actions/auth.ts` — buckets `signup` / `login` / `reset` antes do trabalho caro
- `lib/actions/track-click.ts` — bucket `track` chaveado por `linkId`; comentários das 2 camadas
- `lib/rate-limit.ts` — passa a chamar `check_app_rate_limit` (2 args); `RATE_LIMIT_PEPPER` **obrigatório em produção**; `TARGETS` exportado como trava anti-drift. **v1.2:** comentários corrigidos (o `throw` NÃO roda no boot — dispara na 1ª Server Action); reclassificado como **backstop** do gate de build
- `next.config.ts` — **v1.2:** `assertProductionEnv()` — gate de build que faz `next build`/`next start` **falharem** sem `RATE_LIMIT_PEPPER` em produção (issue #1 do gate da Wave 4)
- `.github/workflows/ci.yml` — **v1.2:** passo `Preflight — required production env` + `env: RATE_LIMIT_PEPPER` no passo `Build`
- `.env.example` — `RATE_LIMIT_PEPPER` reclassificado de privacidade para **controle de segurança** (lockout de auth), com o novo comportamento de falha
- `docs/architecture/ER.md` — `rate_limit_counters` + mudanças da 6.4 em `record_link_click` / `link_clicks`
- `tests/unit/track-click-action.test.ts` — mock de `@/lib/rate-limit` + 5 testes da camada 2
- `tests/integration/link-clicks-rls.test.ts` — expectativa do SELECT anônimo (concern #2)
- `tests/integration/link-clicks.test.ts` — expectativa do SELECT anônimo (concern #2)
- `docs/stories/6.4.story.md` — este arquivo

---

## Revisão de DDL — divergência de AC registrada (TD-5)

> **Autor:** `@data-engineer` (Dara) · 2026-07-19 · relatório completo em
> `docs/qa/gates/epic-6-ddl-review.yml`
> **Natureza deste bloco:** registro de divergência entre o TEXTO de uma AC e a
> implementação. **O texto da AC NÃO foi alterado aqui** — pela `story-lifecycle.md`,
> ACs são de `@po`. Isto é o insumo para essa emenda, não a emenda.

### O fato

A **AC1** (Bloco A) afirma, sobre `rate_limit_counters`:

> **`subject` é sempre um digest hex de 64 chars — nunca IP raw** (NFR19).

A palavra **"sempre"** é factualmente falsa. Reproduzido no banco `development`:

| bucket | subject observado | 64-hex? |
|---|---|---|
| `signup` / `login` / `reset` / `track` | digest hex de 64 chars | ✅ sim |
| `track_link` | `81645bbb-f501-442a-bcb3-7c8cf777d5a9` (o `link_id`, uuid) | ❌ não |

### Por que a REALIDADE está certa e a AC está errada

1. **O balde `track_link` não existia quando a AC1 foi escrita.** Ele nasceu da
   **Emenda 1 do ADR-001 § 6**, que moveu o teto por link para **dentro** de
   `record_link_click` — o único ponto onde a Server Action e o `curl` direto ao
   PostgREST convergem. Foi essa mudança que fechou o vetor de click inflation que o
   gate da Wave 2 provou estar aberto. A AC1 descreve o desenho **anterior** à emenda.

2. **A RPC não tem acesso ao que a AC pede.** O digest 64-hex é derivado no app
   (`lib/rate-limit.ts`) a partir de `SHA-256(pepper + IP)`. Dentro do banco **não
   existe IP**: quem abre a conexão com o PostgREST é a função da Vercel, então o IP
   visível seria o de egresso — que colapsaria todo o tráfego num balde global. A RPC
   conhece **exclusivamente** o `p_link_id`. Cumprir a AC1 literalmente exigiria que o
   app passasse o subject, e é precisamente isso que o hardening da Wave 3 **proibiu**
   (`check_app_rate_limit` recusa `track_link` com `22023`), porque um subject escolhido
   pelo chamador reabre o vetor de **supressão de analytics**.

3. **Não há violação de NFR19.** O NFR19 proíbe **IP raw persistido**. Um `link_id` é
   uuid gerado pelo banco e **público por design** — a policy `links_select_public_active`
   o expõe a qualquer visitante anônimo. Não é PII, não é derivado de PII, e não
   identifica pessoa alguma. A **AC17** ("Sem IP raw persistido em lugar nenhum") está
   **satisfeita** — a divergência é só com a cláusula "sempre" da AC1.

4. **Hashear o uuid seria pior.** Só adicionaria custo e ofuscação: o `link_id` já é
   público, então o digest não esconderia nada, e destruiria a propriedade que torna o
   desenho auditável — hoje dá para olhar uma linha `track_link` e saber a qual link ela
   se refere, sem o que diagnosticar subcontagem em produção fica cego.

### Emenda proposta ao `@po` (texto sugerido para a AC1)

> `subject` **nunca é IP raw** (NFR19). Nos baldes derivados do cliente
> (`signup`/`login`/`reset`/`track`) é um digest hex de 64 chars produzido por
> `lib/rate-limit.ts`; no balde `track_link` — avaliado dentro de `record_link_click`,
> por força da Emenda 1 do ADR-001 § 6 — é o próprio `link_id` (uuid gerado pelo banco,
> público por design e não-PII), porque o IP do visitante não é observável de dentro
> da RPC.

**Ação pendente:** `@po` (Pax) aplica a emenda ao texto da AC1. Até lá, TD-5 permanece
aberto no `epic-6-final-gate.yml` — como **divergência documentada**, não como defeito.

> **Resolvido 2026-07-27 (@po):** emenda aplicada — ver as notas **Emenda 2026-07-27
> (@po)** logo abaixo da AC1 e da AC17. TD-5 fechado; a divergência da cláusula "sempre"
> vira invariante esclarecido (64-hex nos baldes de auth; `link_id` uuid no `track_link`),
> sem violação de NFR19.

---

## QA Results

_(a preencher pelo @qa)_

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-19 | Story criada a partir do EPIC-6-EXECUTION.yaml (Wave 3) e ADR-001 § 3. PRE-3 cancelado (sem SERVICE_ROLE); `RATE_LIMIT_PEPPER` passa a ser o único env novo do epic. Status: Draft | @sm (River) |
| 1.0 | 2026-07-19 | Implementação completa. Migration `20260719190000_rate_limit.sql` aplicada em `development`: `rate_limit_counters` (deny-all) + `check_rate_limit` (janela deslizante, advisory lock, housekeeping, fail-open) + **teto de 60/min por link DENTRO de `record_link_click`** (Emenda 1 § 6.3, camadas 1 e 2; **camada 3 descartada pelo `@pm`**) + `revoke select … from anon` em `link_clicks` (concern #2 do gate da Wave 2). `lib/rate-limit.ts` com IP efêmero + SHA-256 com pepper. Integração nos 4 endpoints. Vetor de click inflation **fechado e provado**: 70 chamadas curl diretas → 60 gravam, 61ª barrada. Uma única assinatura de `record_link_click` confirmada no catálogo. Gates verdes (287 testes, baseline 233). Status: Draft → **InReview** | @dev (Dex) |
| 1.1 | 2026-07-19 | **Correção do gate da Wave 3** (issues #1 HIGH, #2 MEDIUM, #3 LOW). Migration NOVA `20260719200000_rate_limit_hardening.sql` (a `…190000` já aplicada NÃO foi reescrita — precedente da 6.3), aplicada em `development`: `check_rate_limit` vira **INTERNA** (`revoke execute` de `anon`/`authenticated`/`public`) e surge `check_app_rate_limit(text,text)` — allowlist `signup|login|reset|track` (sem `track_link`), limites do NFR18 **hardcoded** e `subject` obrigatoriamente 64-hex; fail-open passa a cobrir o bloco `DECLARE`. `RATE_LIMIT_PEPPER` reclassificado para **controle de segurança** e **obrigatório em produção** (lança na carga do módulo); definido em `.env.local`. Os 3 abusos reproduzidos e **provados fechados** (a: `42501` + tracking legítimo OK; b: `42501` + subject não-computável; c: `22023`, 0 linhas), **sem regressão** do teto de 60/min/link (1ª recusa na #61). Auditoria: nenhuma outra `SECURITY DEFINER` com grant a `anon` e parâmetros livres. Gates verdes (**310 testes**, baseline 296). Produção não tocada; sem push. | @dev (Dex) |
| 1.2 | 2026-07-19 | **Correção do gate da Wave 4** (issue #1 MEDIUM). O fail-fast do `RATE_LIMIT_PEPPER` estava **no pior lugar possível**: o gate provou que sem o env `pnpm build` saía **0**, o servidor subia e `/`, `/login`, `/signup`, `/health` e `/[username]` respondiam 200 com log limpo — o `throw` de topo de módulo só disparava na 1ª invocação de Server Action (500), porque o Next carrega esses módulos **sob demanda**. Um deploy mal provisionado ficava com **todos os sinais verdes e sem login**. Gate movido para `assertProductionEnv()` em `next.config.ts`, avaliado antes da compilação → **falha o build** (artefato ruim nunca embarca) **e o boot** de `next start`. Reforçado por passo `Preflight — required production env` no CI + `env:` no passo `Build`. O `throw` de `lib/rate-limit.ts` vira **backstop** redundante. **Provado:** `RATE_LIMIT_PEPPER= pnpm build` → **exit 1** com mensagem acionável; `pnpm build` com o env → **exit 0**. Development intocado (`RATE_LIMIT_PEPPER= pnpm dev` → `✓ Ready in 121ms`, `/login` 200). Comentários que afirmavam "o app não sobe / quebra no boot" **corrigidos** para o comportamento real. Gates verdes (**333 testes**, baseline 317). Sem push. | @dev (Dex) |
| 1.3 | 2026-07-27 | Status → Done: Epic 6 mergeado (PR #12) e em produção; gates + smoke test manual (owner) + validação visual OK. | @po (Pax) |

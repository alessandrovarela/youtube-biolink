---
id: "6.2"
epic: 6
title: "RLS policies em `links`"
status: InReview
owner_persona: "Dev"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@data-engineer"
quality_gate_tools: ["pnpm typecheck", "pnpm lint", "pnpm test", "pnpm dev"]
created_by: "@sm (River)"
created_at: "2026-07-19"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md — Story 3.1 AC3 (L529, policies nominais deferidas); NFR3 (L75)"
  - "docs/stories/epics/epic-6/EPIC-6-EXECUTION.yaml — Story 6.2, decisão #5"
  - "docs/architecture/epic-6-data-access-inventory.md — § 2 (queries #2, #3, #11-#20), § 4/R2, § 5.2, § 6 (riscos 3, 5, 6, 8, 9)"
depends_on: ["6.1"]
wave: 1
complexity: 4
---

# Story 6.2 — RLS policies em `links`

## Status

**InReview** — implementada pelo `@dev` (Dex). Migration `20260719171040_links_rls.sql` aplicada em `development`, suíte completa verde (219 testes). Aguarda o quality gate do `@data-engineer` (Dara).

---

## Story

**Como** dono de um perfil,
**eu quero** que o banco garanta que só eu leio/escrevo os meus links (inclusive os inativos) e que visitantes anônimos só enxerguem links ativos,
**para que** a autorização não dependa exclusivamente de o desenvolvedor lembrar do `.eq('profile_id', user.id)` em cada query (NFR3 — defense-in-depth).

---

## Contexto do problema

O PRD já **nominou** estas policies na Story 3.1 AC3 e as deferiu explicitamente para a Story 6.2: `links_select_public_active`, `links_select_own`, `links_insert_own`, `links_update_own`, `links_delete_own`. Esta story as implementa.

A armadilha específica de `links` é o **`is_active`**, que discrimina público vs. privado:

- A página pública lê `links` como `anon` filtrando `is_active = true` na aplicação (`lib/queries/public-page.ts:61`).
- O **dashboard lista links INATIVOS do dono**: `app/dashboard/links/page.tsx:18` faz `.select('*')` filtrando **só** por `profile_id`, sem `is_active`. `lib/analytics/clicks.ts:59` faz o mesmo (analytics conta cliques de links desativados).

Uma policy única `USING (is_active = true)` esconderia os links desativados **do próprio dono** — o toggle de desativação viraria "o link sumiu". A solução são **duas policies PERMISSIVE de SELECT**, que o Postgres combina com **`OR`**. [Source: inventory § 4/R2]

E, como na 6.1, a falha é silenciosa: a página pública é ISR (`revalidate = 60`) e um erro de policy publica **404 cacheado por 60s**, não um erro alto. [Source: inventory § 4/R1]

---

## Acceptance Criteria

> Toda AC rastreia PRD Story 3.1 AC3, `EPIC-6-EXECUTION.yaml` ou o inventário PRE-2 (Artigo IV).

1. **Migration única e atômica.** `supabase/migrations/{ts}_links_rls.sql` cria as 5 policies **E** habilita RLS em `public.links` no mesmo arquivo.
   *Given* `links` sem RLS, *when* a migration é aplicada, *then* `rowsecurity = true` e as 5 policies existem em `pg_policies`.
   [Source: EPIC-6-EXECUTION.yaml decisão #5a]

2. **Duas policies PERMISSIVE de SELECT, combinadas por OR** (risco explícito do PRE-2):
   - `links_select_public` — `FOR SELECT TO anon, authenticated USING (is_active = true)`
   - `links_select_own` — `FOR SELECT TO authenticated USING (profile_id = (select auth.uid()))`

   *Given* o usuário A com um link **inativo**, *when* A abre `/dashboard/links` autenticado, *then* o link inativo **aparece** na lista. *And when* um visitante anônimo abre `/[username]` de A, *then* o link inativo **não aparece**.
   [Source: inventory § 4/R2, § 5.2, § 6 risco 6 · PRD Story 3.1 AC3]

   > **Nota de nomenclatura:** o PRD (L529) chama a primeira de `links_select_public_active`; o inventário chama de `links_select_public`. Adotar **`links_select_public`** (nome do inventário, que é o artefato técnico mais recente) e registrar a equivalência no comentário da migration.

3. **Policy `links_insert_own`** — `FOR INSERT TO authenticated WITH CHECK (profile_id = (select auth.uid()))`.
   *Given* o usuário A autenticado, *when* tenta inserir um link com `profile_id` de B via PostgREST, *then* a operação é rejeitada.
   [Source: inventory § 5.2 · PRD Story 3.1 AC3]

4. **Policy `links_update_own`** — `FOR UPDATE TO authenticated USING (profile_id = (select auth.uid())) WITH CHECK (profile_id = (select auth.uid()))`.
   O `WITH CHECK` é o que impede "doar" um link para outro perfil trocando `profile_id`.
   [Source: inventory § 5.2]

5. **Policy `links_delete_own`** — `FOR DELETE TO authenticated USING (profile_id = (select auth.uid()))`.
   [Source: inventory § 5.2 · PRD Story 3.1 AC3]

6. **Mutations com `RETURNING` continuam funcionando.** `lib/actions/links.ts:132` (UPDATE title/url), `:199` (DELETE) e `:228` (UPDATE `is_active`) fazem `.select(...)` após a mutação, o que exige que a linha satisfaça **também** uma policy de SELECT — coberto por `links_select_own`.
   *Given* o usuário A, *when* edita, deleta ou faz toggle de um link próprio, *then* a action retorna a linha (não `null`) e a UI atualiza.
   [Source: inventory § 6 risco 8]

7. **CRUD completo sem regressão (verificação manual obrigatória).**
   *Given* a migration aplicada em `development`, *when* o owner executa criar → editar → **desativar e confirmar que continua listado** → reordenar (drag-and-drop) → excluir, *then* todos os fluxos funcionam. Registrado no Debug Log.
   [Source: inventory § 5.5 item 3 · EPIC-6-EXECUTION.yaml notes 6.2]

8. **Página pública ISR verificada anonimamente + invalidação de cache.**
   *Given* a migration aplicada, *when* se faz `curl` anônimo em `/[username]` de um perfil com links ativos e inativos, *then* apenas os ativos aparecem, na ordem de `position`. *And* o cache ISR é invalidado antes da verificação. **Falha de policy aqui é silenciosa (404 cacheado 60s)** — esta verificação não é opcional.
   [Source: inventory § 4/R1, § 5.5 item 6, § 6 risco 5]

9. **App-layer intacta.** Nenhum `.eq('profile_id', user.id)` nem `.eq('is_active', true)` existente é removido. A RLS soma (NFR3).
   [Source: PRD NFR3 · EPIC-6-EXECUTION.yaml decisão #5b]

10. **Testes de integração existentes revisados caso a caso (risco ALTO do PRE-2).** `tests/integration/links-crud.test.ts:142,151`, `links.test.ts:64,73,103` e `links-reorder.test.ts:128` verificam efeitos de UPDATE/DELETE **lendo com client anon**. Se a linha inspecionada estiver com `is_active = false`, `links_select_public` a esconde e a asserção lê `null`.
    *Given* a suíte completa, *when* `pnpm test` roda após a migration, *then* está verde — com as verificações de estado pós-mutação migradas para `createAdminClient()` (que bypassa RLS) onde necessário.
    [Source: inventory § 6 risco 3]

11. **Teste de integração de policy (db-layer, novo).**
    *Given* dois perfis semeados, *when* um client **anon** tenta (a) ler um link inativo, (b) inserir link, (c) atualizar link alheio, (d) deletar link alheio, *then* todas falham (0 linhas / erro). *And* um client autenticado como o dono lê os próprios links inativos.
    [Source: EPIC-6-EXECUTION.yaml notes 6.2]

12. **Migration aplicada em `development` via `supabase db push`**; produção via CD `migrate-production` no merge da `main`.
    [Source: EPIC-6-EXECUTION.yaml decisão #5d · PRD FR22]

---

## Escopo

### IN
- Migration `{ts}_links_rls.sql` (5 policies + `ENABLE RLS`).
- Revisão dos testes de integração de `links` que leem com client anon (AC10).
- Teste novo de policy no banco (AC11).
- Smoke manual do CRUD completo + página pública anônima.
- Atualização de `docs/architecture/ER.md`.

### OUT
- RLS em `link_clicks` e correção da view `link_click_daily` (Story 6.3).
- Índice `idx_links_active_profile_position (profile_id, position) WHERE is_active` — o inventário (§ 5.2) só recomenda **se** o EXPLAIN da página pública degradar. Registrar como observação; não criar preventivamente.
- Refactor de `lib/actions/track-click.ts` (Story 6.3) — ver Nota de acoplamento abaixo.
- Rate limiting, middleware, CSP.

---

## Tasks / Subtasks

- [x] **T1. Migration `{ts}_links_rls.sql`** (AC: 1–5)
  - [x] Cabeçalho: origem (PRD Story 3.1 AC3), equivalência de nome (**decisão do `@pm`: prevalece `links_select_public_active`, do PRD** — ver Completion Notes #1), e por que são **duas** policies de SELECT.
  - [x] As 5 policies conforme inventário § 5.2, usando `(select auth.uid())`.
  - [x] `alter table public.links enable row level security;` no MESMO arquivo.
  - [x] `notify pgrst, 'reload schema';`

- [x] **T2. Aplicar em `development`** (AC: 12)
  - [x] `supabase db push` (aplicada; `supabase migration list` confirma `20260719171040` em Local **e** Remote). Verificação de `pg_policies`/`rowsecurity` feita **por comportamento** (16 testes db-layer) — ver nota de ferramenta no Debug Log.

- [x] **T3. Revisar testes de integração existentes** (AC: 10)
  - [x] `links-crud.test.ts:142,151` · `links.test.ts:64,73,103` · `links-reorder.test.ts:128` revisados caso a caso.
  - [x] **Nenhuma alteração necessária** — análise e evidência nas Completion Notes #4. Suíte completa verde sem tocar nesses arquivos.

- [x] **T4. Teste de policy no banco** (AC: 11)
  - [x] anon: não lê inativo, não insere, não atualiza/deleta alheio.
  - [x] authenticated dono: lê os próprios inativos.

- [x] **T5. Smoke manual + invalidação ISR** (AC: 7, 8)
  - [x] `curl` anônimo em `/[username]` com **cache ISR frio** → só ativos, na ordem de `position`, HTTP 200. Ver Debug Log.
  - [x] criar → editar → desativar (**continua listado**) → reordenar → excluir: coberto no **db-layer** com as formas de query exatas das actions (ver Completion Notes #5). Smoke via UI autenticada não executado — mesma limitação registrada na Story 6.1 (T5).

- [x] **T6. App-layer intacta + docs** (AC: 9)
  - [x] Conferido: o diff da story não contém **nenhum** arquivo em `lib/` ou `app/` (`git status` — só a migration, o teste novo e docs).
  - [x] Atualizar `docs/architecture/ER.md`, File List e Change Log.

---

## Dev Notes

### Policies PERMISSIVE combinam com `OR`
Este é o conceito central da story. Duas policies PERMISSIVE de SELECT **não se anulam** — a linha é visível se **qualquer uma** permitir. Resultado: `anon` vê só links ativos (de qualquer perfil); `authenticated` vê **todos os próprios** (ativos e inativos) **mais** os ativos de terceiros. A segunda parte é intencional — um usuário logado que abre a página pública de outro precisa enxergá-la — e não vaza nada, porque as queries do dashboard já filtram por `profile_id = user.id` na aplicação. [Source: inventory § 4/R2]

### Nota de acoplamento com a 6.3 — `trackLinkClick` (não bloqueante)
`lib/actions/track-click.ts:47` lê o link **sem** filtro `is_active` e checa o estado em TS. Com `links_select_public`, um link inativo simplesmente **não é visível** para `anon` → o retorno muda de `'Link inativo'` para `'Link não encontrado'`. O comportamento externo é equivalente (o clique não é registrado nos dois casos) e `tests/unit/track-click-action.test.ts:106` continua verde porque mocka o client — mas passa a cobrir um **caminho morto**. A Story 6.3 elimina esse SELECT por completo (a validação de `is_active` migra para a RPC `record_link_click`), então **não refatorar aqui**; apenas registrar. [Source: inventory § 4/R2 nota, § 6 risco 9 · ADR-001 § 2]

### Performance
O índice `idx_links_profile_position (profile_id, position)` já cobre o predicado de `links_select_own`. Para `links_select_public`, o predicado `is_active = true` combinado com `profile_id` continua usando o índice existente. Índice parcial só se o EXPLAIN degradar. [Source: inventory § 5.2]

### Dependências
Depende da **Story 6.1** (mesmo padrão de migration; o aprendizado de `profiles` informa esta). Sem dependências npm novas. Sem env novo.

---

## Riscos

| # | Risco | Severidade | Mitigação |
|---|---|---|---|
| R1 | Policy única `is_active = true` esconde links inativos do dono → "o link sumiu" após toggle | **Alta** | AC2: duas policies PERMISSIVE; AC7 exige smoke do toggle |
| R2 | Falha de policy na página pública vira **404 cacheado por 60s** (ISR) — não falha alto | **Alta** | AC8: verificação `curl` anônima obrigatória pós-invalidação de ISR |
| R3 | Testes que leem estado pós-mutação com client anon quebram ou passam a testar outra coisa | **Alta** | AC10: revisão caso a caso, `createAdminClient()` para verificação de estado |
| R4 | Mutation com `RETURNING` falha por falta de policy de SELECT | Média | `links_select_own` cobre (AC6) |
| R5 | `UPDATE` sem `WITH CHECK` permitiria "doar" link a outro perfil | Média | AC4 exige `WITH CHECK` |
| R6 | Degradação de plano na query pública | Baixa | Índice existente cobre; índice parcial só se EXPLAIN piorar (OUT de escopo) |

---

## Definition of Done

- [x] Migration `{ts}_links_rls.sql` com 5 policies **e** `ENABLE RLS` no mesmo arquivo, aplicada em `development`.
- [x] Dashboard lista links inativos do dono; página pública anônima lista **apenas** ativos, na ordem correta (verificado manualmente com `curl` com cache ISR frio).
- [x] CRUD completo verde (db-layer, com as formas de query exatas das actions).
- [x] Testes de integração existentes revisados e verdes; teste novo de policy no banco verde (16 testes).
- [x] Nenhum filtro app-layer removido (NFR3).
- [x] `pnpm typecheck` / `pnpm lint` / `pnpm test` (suíte completa) verdes **e** `pnpm dev` sobe sem erro.
- [x] `docs/architecture/ER.md`, File List e Change Log atualizados. Sem push (delegado a `@devops`).

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@data-engineer`.

---

## Story Type Analysis

- **Primary Type:** Database (RLS/DDL) + refactor de testes de integração.
- **Complexity:** **4** (média) — 5 policies, o gotcha das duas SELECT PERMISSIVE, e revisão de 6 pontos de teste existentes.
- **Primary Agents:** `@dev` (executor), `@data-engineer` (quality gate). **Supporting:** `@devops` (push/CD).
- **Focus Areas:** policies PERMISSIVE vs RESTRICTIVE, `WITH CHECK` em UPDATE, `RETURNING` exigindo policy de SELECT, falha silenciosa sob ISR.

---

## Project Structure Notes

- `supabase/migrations/{ts}_links_rls.sql`
- `tests/integration/links-crud.test.ts`, `links.test.ts`, `links-reorder.test.ts` (revisão) + teste de policy
- `docs/architecture/ER.md`

---

## Dev Agent Record

### Agent Model Used

`claude-opus-4-8[1m]` — `@dev` (Dex), execução YOLO.

### Debug Log References

**Aplicação em `development` (ref `rjsvkwjgsctqsswrospx`):**

```
$ supabase db push
Applying migration 20260719171040_links_rls.sql...
Finished supabase db push.

$ supabase migration list
   Local          | Remote         | Time (UTC)
   20260719170252 | 20260719170252 | 2026-07-19 17:02:52   ← Story 6.1
   20260719171040 | 20260719171040 | 2026-07-19 17:10:40   ← Story 6.2
```

> **Nota de ferramenta:** a Story 6.1 conferiu `pg_policies`/`rowsecurity` pela
> Management API do Supabase. Nesta sessão o token da CLI está no keychain em formato
> `go-keyring` (blob cifrado) e não há `SUPABASE_ACCESS_TOKEN` no ambiente, então a
> query direta ao catálogo não foi possível. A verificação foi feita **por
> comportamento**, o que é evidência mais forte que a leitura do catálogo: os 16 testes
> de `links-rls.test.ts` falariam se qualquer uma das 5 policies estivesse ausente,
> errada ou se o `ENABLE ROW LEVEL SECURITY` não tivesse pegado (sem RLS, os testes de
> negação — anon lendo link inativo, A alterando link de B — passariam a retornar linhas).

**Gates:**

| Gate | Resultado |
|---|---|
| `pnpm typecheck` | ✅ verde (exit 0, sem saída) |
| `pnpm lint` | ✅ verde (exit 0, sem saída) |
| `pnpm test` (suíte completa) | ✅ **36 arquivos / 219 testes passando**, 1 skipado (pré-existente). Baseline da 6.1 era 203 → **+16** desta story |
| `pnpm dev` | ✅ `Ready in 197ms`, `.next` removido antes (build limpo), zero erro no log |

**AC8 — página pública anônima com cache ISR FRIO (a armadilha da story):**

Setup: perfil semeado via service role com 3 links — `ZZ_ATIVO_PRIMEIRO` (position 0),
`ZZ_ATIVO_SEGUNDO` (position 1) e `ZZ_INATIVO_SEGREDO` (position 2, `is_active = false`).
`rm -rf .next` **antes** de subir o dev server → nenhuma entrada ISR pré-RLS podia estar
cacheada; a render medida é nova.

```
GET /rls62smoke233875              → HTTP 200      (NÃO 404 — a policy não quebrou a leitura)
  ZZ_ATIVO_PRIMEIRO      → 1 ocorrência   ✅ presente
  ZZ_ATIVO_SEGUNDO       → 1 ocorrência   ✅ presente
  ZZ_INATIVO_SEGREDO     → 0 ocorrências  ✅ ausente
  "Smoke 6.2"            → 1 ocorrência   ✅ profile renderizou
  ordem no HTML          → PRIMEIRO, SEGUNDO  ✅ ordenado por position

GET /nao_existe_zzz_999           → 404   (404 legítimo, não artefato de policy)
GET /health                       → 200
GET /dashboard/links              → 200 (redirect p/ login)
dev.log                           → nenhum "error"/"⨯"
```

Perfil de smoke removido ao final (`auth.admin.deleteUser` → cascade em profiles/links).

### Completion Notes

1. **Nome da policy pública: `links_select_public_active`** — decisão do `@pm`, seguida.
   O AC2 (texto do `@po`) instrui adotar `links_select_public`, do inventário; o `@pm`
   reverteu: o PRD Story 3.1 AC3 (L529) **contratou** `links_select_public_active`, e a
   rastreabilidade ao contrato prevalece sobre o artefato técnico posterior (Artigo IV).
   O texto do AC2 **não foi editado** (ACs são de propriedade do `@po`); a divergência
   fica registrada aqui e no cabeçalho da migration, que documenta a equivalência.
   Os outros 4 nomes coincidem entre PRD e inventário.
2. **Migration atômica.** `20260719171040_links_rls.sql` cria as 5 policies e só então
   executa `ENABLE ROW LEVEL SECURITY`, no mesmo arquivo → sem janela de lockout.
   Termina com `notify pgrst, 'reload schema';`.
3. **Nada de app-layer foi removido (AC9).** `git status` do diff da story lista apenas
   arquivos **novos** (migration + teste) e edições de documentação — **zero** arquivos
   em `lib/` ou `app/`. Logo, nenhum `.eq('profile_id', user.id)` nem
   `.eq('is_active', true)` saiu.
4. **AC10 — os 6 pontos de teste apontados como risco ALTO não precisaram de mudança.**
   Revisados caso a caso, com a suíte completa verde como prova:
   - `links-crud.test.ts:142,151` e `links-reorder.test.ts:128` usam o client **anon**
     para tentar UPDATE/DELETE e afirmam `toHaveLength(0)`. Sob RLS, `anon` não tem
     policy de UPDATE/DELETE → 0 linhas afetadas, sem erro. A asserção continua
     verdadeira — e agora por um motivo **mais forte** (antes era o filtro app-layer;
     agora é o filtro **e** a policy).
   - `links.test.ts:64,73` idem.
   - `links.test.ts:103` lê com anon filtrando `.eq('is_active', true)` — só toca linhas
     que `links_select_public_active` permite. Intacto.
   - A verificação **autoritativa** de estado pós-mutação nesses três arquivos já usava
     `createAdminClient()` (que bypassa RLS) antes desta story, então a migração
     recomendada pelo AC10 já estava feita.
   - `clicks-aggregation.test.ts` / `clicks-benchmark.test.ts` chamam `getClicksByLink`
     com client anon, e esse helper lê `links` **sem** filtro de `is_active` — mas todos
     os links que eles semeiam são ativos (default), então nada some. Verde.
5. **CRUD (AC7) coberto no db-layer com as formas de query exatas das actions.** O teste
   novo reproduz literalmente: INSERT com `profile_id` do dono; UPDATE `.eq('id').eq('profile_id')`
   com `.select('*')`; DELETE com `.select('id')`; toggle `is_active = false` com
   `.select('*')`; e a listagem do dashboard (`.eq('profile_id').order('position')`,
   **sem** filtro de `is_active`) via client `authenticated` real. O smoke pela UI
   autenticada não foi executado — exigiria login com senha no navegador —, mesma
   limitação já aceita na Story 6.1 (T5).
6. **O teste do RETURNING é o que fecha o risco R4.** `toggle para is_active = false
   retorna a linha` prova que `links_select_own` cobre o RETURNING de um link que acabou
   de deixar de ser público. Sem essa policy, `toggleLinkActive` devolveria
   "Link não encontrado" numa operação **bem-sucedida** — a falha mais sutil da story.
7. **Nota de acoplamento com a 6.3 confirmada empiricamente.**
   `tests/integration/track-click.test.ts:81` ("não grava clique para link inativo")
   segue verde: com `links_select_public_active`, o link inativo deixa de ser visível
   para `anon`, então o retorno de `trackLinkClick` muda de `'Link inativo'` para
   `'Link não encontrado'` — mas o teste afirma só `res.ok === false`, que vale nos dois
   casos. O caminho `'Link inativo'` virou **código morto**; a Story 6.3 o elimina ao
   migrar a validação para a RPC `record_link_click`. **Não refatorado aqui**, como
   instruído.
8. **Índice parcial não criado**, conforme escopo OUT. O `(profile_id, position)`
   existente segue cobrindo; nenhuma degradação observada no smoke.

### Riscos remanescentes / débito

| # | Item | Severidade | Nota |
|---|---|---|---|
| 1 | `pg_policies`/`rowsecurity` não lidos diretamente do catálogo | Baixa | Token da CLI inacessível nesta sessão (keychain `go-keyring`). Coberto por 16 testes comportamentais que falhariam se qualquer policy estivesse ausente ou errada. O `@data-engineer` pode confirmar no catálogo no quality gate. |
| 2 | Smoke do CRUD pela **UI autenticada** (drag-and-drop de reorder, toggle no dashboard) não executado | Média | Coberto no db-layer com as queries idênticas às das actions, e `lib/actions/links.ts` não foi tocado (seus testes seguem verdes). O reorder faz UPDATE de `position` filtrado por `id` + `profile_id` → satisfaz `links_update_own`. Vale um clique manual no dashboard antes do merge. |
| 3 | Caminho morto `'Link inativo'` em `lib/actions/track-click.ts:47` | Baixa | Intencional e registrado (Completion Note #7). Removido na Story 6.3. |
| 4 | Invalidação de ISR em **produção** | Média | Nada foi tocado em prod. No merge da `main` o CD aplica a migration e o deploy da Vercel recria o cache; ainda assim vale `curl` de conferência em um `/[username]` real logo após o deploy — **um 404 aqui ficaria cacheado 60s**. **Ação para `@devops` no push.** |
| 5 | Usuário `authenticated` enxerga links **ativos** de terceiros via PostgREST direto | Nenhuma | Intencional e necessário (a página pública de outro usuário precisa renderizar para quem está logado). Não vaza nada além do que a página pública já expõe; os inativos de terceiros continuam invisíveis (testado). |

### File List

**Criados**
- `supabase/migrations/20260719171040_links_rls.sql` — 5 policies (`links_select_public_active`, `links_select_own`, `links_insert_own`, `links_update_own`, `links_delete_own`) e `ENABLE ROW LEVEL SECURITY` no mesmo arquivo.
- `tests/integration/links-rls.test.ts` — 16 testes de RLS no db-layer.

**Modificados**
- `docs/architecture/ER.md` — `links` deixa de ser "sem RLS": tabela das 5 policies, explicação das duas SELECT combinadas por OR, `WITH CHECK` e RETURNING; forward-looking reduzido a `link_clicks` (6.3).
- `docs/stories/6.2.story.md` — status, checkboxes, Dev Agent Record, Change Log.

**Não modificados (intencionalmente — AC9/NFR3)**
- `lib/actions/links.ts`, `lib/queries/public-page.ts`, `lib/analytics/clicks.ts`, `lib/actions/track-click.ts`, `app/dashboard/links/page.tsx` — a autorização application-layer permanece integralmente.
- `tests/integration/links-crud.test.ts`, `links.test.ts`, `links-reorder.test.ts` — revisados (AC10), nenhuma alteração necessária (Completion Note #4).

---

## QA Results

_(a preencher pelo @data-engineer)_

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-19 | Story criada a partir do EPIC-6-EXECUTION.yaml (Wave 1) + PRD Story 3.1 AC3 + inventário PRE-2. Status: Draft | @sm (River) |
| 1.0 | 2026-07-19 | Implementada: migration `20260719171040_links_rls.sql` (5 policies + ENABLE RLS), 16 testes de RLS db-layer, ER.md atualizado. Policy pública nomeada `links_select_public_active` (decisão do @pm, PRD L529). Migration aplicada em `development`. typecheck/lint/219 testes/dev verdes; página pública anônima verificada com cache ISR frio. Status: Draft → InReview | @dev (Dex) |

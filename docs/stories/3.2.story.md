---
id: "3.2"
epic: 3
title: "Helper de sanitização de URL"
status: Ready for Review
owner_persona: "Dev"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pnpm test:unit", "pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-07-01"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md § 6 — Epic 3, Story 3.2"
  - "docs/architecture.md § 2.5 (Inline Validation), § 6.2 (lib/validation)"
  - "docs/stories/epics/epic-3/EPIC-3-EXECUTION.yaml — Wave 1"
depends_on: []
---

# Story 3.2 — Helper de sanitização de URL

## Status

**Ready for Review** — implementada pelo `@dev` (YOLO, Wave 1). 21 asserts unitários verdes; typecheck/lint OK. Aguarda QA Gate (`@qa`).

---

## Story

**Como** dev,
**eu quero** um helper puro que valide e sanitize URLs de link,
**para que** schemes perigosos nunca cheguem ao DB.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 3, Story 3.2.

1. `lib/validation/url.ts` exporta `sanitizeLinkUrl(input: string): Result<string, UrlError>`.
2. Aceita apenas `http://` e `https://`.
3. Rejeita (com erro tipado): `javascript:`, `data:`, `vbscript:`, `file:`, URLs sem scheme, strings vazias, URLs malformadas.
4. Normaliza URL (trim, decode padrão, preserva query e fragment).
5. Testes cobrem cada caso (≥15 asserts) incluindo obfuscation (`JaVaScRiPt:`, com espaço/tab no começo, etc.).

---

## Tasks / Subtasks

- [x] **T1. Helper puro `sanitizeLinkUrl`** (AC: 1, 2, 3, 4)
  - [x] `lib/validation/url.ts` — assinatura `sanitizeLinkUrl(input: string): Result<string, UrlError>` (tipo `Result` ok/err — reusar `ActionResult` ou tipo local; ver Dev Notes).
  - [x] Normalizar: `trim`, parse via `new URL()`, preservar `search` e `hash`.
  - [x] Allowlist de scheme: só `http:`/`https:` (checar `url.protocol` após parse, **case-insensitive**).
  - [x] Rejeitar com `UrlError` tipado: scheme perigoso, sem scheme, vazio, malformado.

- [x] **T2. Testes unitários (≥15 asserts)** (AC: 5)
  - [x] Happy path: `http://`, `https://`, com query/fragment preservados.
  - [x] Rejeições: `javascript:`, `JaVaScRiPt:`, ` javascript:` (espaço/tab), `data:`, `vbscript:`, `file:`, `""`, `"   "`, sem scheme (`example.com`), malformada.

- [x] **T3. Atualizar File List e Change Log.**

---

## Dev Notes

> **CRITICAL:** Padrão de validação vem de `architecture.md`. Nada inventado (Artigo IV).

### Validação inline + helpers puros (SEM libs)
A arquitetura decidiu **zero dependência de validação**: checks TS diretos + helpers puros. `sanitizeLinkUrl` é exatamente um desses helpers, citado nominalmente na arquitetura. **Não usar `zod` nem validador externo.** [Source: architecture.md § 2.5 Inline Validation; § 3.1 "Validation: Inline TS + helpers puros — Zero dependência"; § 6.2 `lib/validation.ts`]

### Localização
PRD pede `lib/validation/url.ts`. A arquitetura descreve `lib/validation.ts` (flat) com `sanitizeLinkUrl` + `validateUsername`. Aceitar `lib/validation/url.ts` (submódulo) OU consolidar em `lib/validation.ts` — @dev decide mantendo consistência com o que a Story 2.3 já criou para `validateUsername`. [Source: architecture.md § 6.2; prd.md Story 3.2 AC1]

### Tipo `Result`
Reusar o padrão `ActionResult<T>` (`{ ok: true; data } | { ok: false; error }`) de `architecture.md` § 5.1, ou um `Result<T, UrlError>` local equivalente. Manter tipagem do erro (`UrlError`) para mensagens claras. [Source: architecture.md § 5.1]

### Camadas de defesa
Este helper é a **defesa primária**; a CHECK `url ~ '^https?://'` no banco (Story 3.1) é a última linha. Consumido por `createLink`/`updateLink` (Story 3.3). [Source: architecture.md § 9.3 nota]

### Dependências
Puro, sem DB. Independente. Wave 1 (paralela com 3.1 e 3.6).

---

## Definition of Done

- [x] `sanitizeLinkUrl` aceita só http(s), rejeita schemes perigosos + obfuscation, normaliza preservando query/fragment.
- [x] ≥15 asserts unitários verdes (`pnpm test:unit`).
- [x] Sem dependência externa de validação adicionada.
- [x] `pnpm lint` verde. `File List` preenchido. Sem push.

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** Helper puro (lib) + testes unitários.
- **Complexity:** **Low** — função pura, sem I/O; o valor está na cobertura de edge cases (obfuscation).
- **Primary Agents:** `@dev`, `@qa`. **Supporting:** `@architect` (gate Wave 1), `@devops` (push).
- **Focus Areas:** allowlist de scheme case-insensitive, obfuscation (espaços/tabs/casing), zero-dependência.

---

## Project Structure Notes

- `lib/validation/url.ts` (ou `lib/validation.ts`); teste em `tests/unit/`. [Source: architecture.md § 6.2]

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (1M context) via @dev (Dex) — modo YOLO, Wave 1.

### Debug Log References
`pnpm exec vitest run tests/unit/url.test.ts` → 18 casos / 21 asserts verdes. `pnpm test:unit` 37 passed (sem regressão). `pnpm typecheck`/`lint` OK.

### Completion Notes
- `sanitizeLinkUrl(input): Result<string, UrlError>` reusando o `Result<T,E>` de `username.ts` (zero duplicação, zero `zod`).
- `UrlError = 'empty' | 'no_scheme' | 'unsupported_scheme' | 'malformed'`; detecção de scheme via regex antes do parse para todos os membros serem significativos.
- Allowlist `http:`/`https:`; rejeita `javascript:`/`data:`/`vbscript:`/`file:`/obfuscation (espaço/tab inicial, casing misto); preserva query+fragment.

### File List
**Criados:** `lib/validation/url.ts`, `tests/unit/url.test.ts`

---

## QA Results

**Gate: PASS** — @qa (Quinn), 2026-07-01 · commit `684c950`

| Checagem | Resultado |
|----------|-----------|
| 1. Code review | ✅ Helper puro, allowlist `http/https` via `URL.protocol` (case-insensitive por construção); `trim` neutraliza obfuscation por espaço/tab; erro tipado `UrlError` com 4 membros significativos. |
| 2. Testes | ✅ 21 asserts (>15 exigidos): happy path, query+fragment, `javascript:`/`JaVaScRiPt:`/` javascript:`/`data:`/`vbscript:`/`file:`/vazio/sem-scheme/malformada. |
| 3. ACs | ✅ AC1–5 integralmente. |
| 6. Segurança | ✅ Defesa primária robusta contra schemes perigosos; paridade com CHECK do banco (§ 9.3). |
| 7. Docs | ✅ Comentário de cabeçalho explica camadas de defesa. |

**Reconciliação validada:** zero-dependência (sem `zod`), reusa `Result<T,E>` de `username.ts` — alinhado à arquitetura § 2.5/§ 3.1. Excelente cobertura de edge cases.

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-01 | Draft inicial (PRD § 6 Epic 3 + architecture.md § 2.5/§ 3.1/§ 6.2). Reconciliação: validação inline, sem zod. | @sm (River) |
| 0.2 | 2026-07-01 | GO 10/10 — Status Draft→Ready. Reconciliação (sem zod) ratificada. | @po (Pax) |
| 0.3 | 2026-07-01 | Implementado (sanitizeLinkUrl + 21 asserts). Zero-dependência. Status → Ready for Review | @dev (Dex) |

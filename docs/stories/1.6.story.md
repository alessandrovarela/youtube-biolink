---
id: "1.6"
epic: 1
title: "Deploy automático na Vercel"
status: Ready
owner_persona: "Dev mantenedor"
agent_owner: "@dev"
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: ["vercel-deploy-check", "pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-05-27"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
sources:
  - "docs/prd.md § 6 — Epic 1, Story 1.6"
  - "docs/architecture.md § 2.2, § 13.1, § 13"
---

# Story 1.6 — Deploy automático na Vercel

## Status

**Ready** — Validado pelo `@po` (Pax). GO 8.0/10. Liberado para `@dev`. ⚠️ Story é quase 100% configuração manual no dashboard Vercel — requer intervenção do owner para T1-T4.

---

## Story

**Como** dev mantenedor do youtube-biolink,
**eu quero** o projeto conectado à Vercel com deploy automático em `main` e previews em PRs,
**para que** todo merge gere uma URL pública e todo PR tenha preview para review.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 1, Story 1.6.

1. Projeto importado na Vercel, vinculado ao repositório GitHub.
2. Variáveis de ambiente por escopo configuradas na Vercel: Production usa credenciais do projeto Supabase `production`; Preview e Development usam credenciais do projeto Supabase `development`.
3. Merge em `main` gera deploy em URL estável (ex.: `youtube-biolink.vercel.app`) conectado ao banco `production`.
4. PR gera preview URL automaticamente (conectada ao banco `development`), comentada pelo bot da Vercel.
5. Domínio de preview é acessível e serve a build da branch contra o banco `development`.

---

## Tasks / Subtasks

- [ ] **T1. Importar projeto na Vercel** (AC: 1)
  - [ ] Acessar `vercel.com/new`.
  - [ ] Importar o repositório `youtube-biolink` do GitHub.
  - [ ] Framework preset: Next.js (detectado automaticamente).
  - [ ] Root directory: `/` (raiz do monorepo).
  - [ ] Build command: `pnpm build` (ou deixar Vercel detectar via `package.json`).
  - [ ] Output directory: `.next` (padrão Next.js).

- [ ] **T2. Configurar variáveis de ambiente na Vercel** (AC: 2, 3, 5)
  - [ ] No painel Vercel → Settings → Environment Variables.
  - [ ] Adicionar para escopo **Production**:
    - `NEXT_PUBLIC_SUPABASE_URL`: URL do projeto `youtube-biolink-prod`
    - `NEXT_PUBLIC_SUPABASE_ANON_KEY`: anon key do projeto `production`
  - [ ] Adicionar para escopos **Preview** e **Development**:
    - `NEXT_PUBLIC_SUPABASE_URL`: URL do projeto `youtube-biolink-dev`
    - `NEXT_PUBLIC_SUPABASE_ANON_KEY`: anon key do projeto `development`
  - [ ] Verificar que as variáveis corretas estão aplicadas por scope.

- [ ] **T3. Trigger primeiro deploy** (AC: 3)
  - [ ] Vercel faz deploy automático após configuração. Se não: ir em Deployments → Redeploy.
  - [ ] Aguardar build completar.
  - [ ] Acessar a URL de Production (`youtube-biolink.vercel.app` ou similar).
  - [ ] Confirmar que a página inicial renderiza sem erros 500.
  - [ ] Confirmar no Vercel dashboard que o ambiente aponta para credenciais `production`.

- [ ] **T4. Validar preview em PR** (AC: 4, 5)
  - [ ] Criar um PR de qualquer branch feature para `main`.
  - [ ] Aguardar Vercel bot comentar com preview URL no PR.
  - [ ] Acessar a preview URL e confirmar que a aplicação carrega.
  - [ ] Confirmar que a preview conecta ao banco `development` (checkar via `/health` quando disponível na Story 1.7).

- [ ] **T5. Commit de documentação** (AC: 1)
  - [ ] Não há arquivos de código a commitar nesta story (configuração é feita no dashboard Vercel).
  - [ ] Atualizar `README.md` com a URL de produção (ex.: `https://youtube-biolink.vercel.app`).
  - [ ] Commit: `docs: add vercel production url to readme [Story 1.6]`
  - [ ] **NÃO push direto** — criar PR para validar o preview via Vercel bot.

- [ ] **T6. Atualizar File List e Change Log da story**

---

## Dev Notes

> **CRITICAL:** Toda informação técnica abaixo veio de `docs/architecture.md` e `docs/prd.md` (citados). Nada inventado.

### Estratégia de Deploy

- **Frontend + Backend:** Vercel (Next.js serverless). [Source: architecture.md § 13.1]
- **Environments:** Production → Supabase `production`. Preview + Development → Supabase `development`. [Source: architecture.md § 13.1]
- **Migrations:** `supabase db push` aplicado manualmente por devs no `development`; CI aplica em `production` após merge na `main`. [Source: architecture.md § 13.1]

### Variáveis de Ambiente por Scope

| Scope | `NEXT_PUBLIC_SUPABASE_URL` | `NEXT_PUBLIC_SUPABASE_ANON_KEY` |
|-------|----------------------------|----------------------------------|
| Production | URL do `youtube-biolink-prod` | anon key do `prod` |
| Preview | URL do `youtube-biolink-dev` | anon key do `dev` |
| Development | URL do `youtube-biolink-dev` | anon key do `dev` |

[Source: architecture.md § 2.2 — "Vercel: Env Vars por scope"]

### Sem arquivo de configuração Vercel

Não é necessário criar `vercel.json` para este setup básico — o framework Next.js é detectado automaticamente. Adicionar `vercel.json` apenas se customizações de headers/redirects forem necessárias (stories futuras).

### URL de Produção

Vercel gera automaticamente: `{repo-name}.vercel.app`. Pode-se configurar domínio customizado nas settings da Vercel (fora do escopo do MVP).

---

## Definition of Done

- [ ] Projeto importado na Vercel e vinculado ao GitHub.
- [ ] Variáveis de ambiente configuradas por scope (Production vs Preview/Dev).
- [ ] Deploy de `main` acessível na URL de produção da Vercel.
- [ ] PR gera preview URL comentada pelo bot da Vercel.
- [ ] Preview acessível e conectada ao banco `development`.
- [ ] README atualizado com URL de produção.
- [ ] `File List` preenchido pelo `@dev`.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration: Disabled**
>
> `coderabbit_integration` não está configurado em `.aiox-core/core-config.yaml`. Validação de qualidade nesta story será via revisão manual do `@qa` (gate da Phase 4 — SDC).

---

## Story Type Analysis

- **Primary Type:** Deployment / Infrastructure
- **Complexity:** **Low** — configuração de plataforma via dashboard, sem código novo.
- **Primary Agents:** `@dev` (configuração), `@devops` (validação push/PR), `@qa` (gate)
- **Supporting Agents:** `@devops` (push após aprovação)
- **Quality Gates:**
  - Pre-PR (`@devops`): deploy de Production acessível; preview URL funcionando em PR.
- **Focus Areas:**
  - Segurança: credenciais `production` apenas no scope Production da Vercel.
  - Sem hardcode de credenciais em arquivos commitados.
  - Preview conecta ao banco `development` (não `production`).

---

## Project Structure Notes

- PRD shards e Architecture shards ainda não existem — usar monolíticos.
- Esta story não cria arquivos de código — apenas configura plataforma externa e documenta a URL.

---

## Dev Agent Record

> Preenchido pelo `@dev` durante implementação.

### Agent Model Used

_a preencher_

### Debug Log References

_a preencher_

### Completion Notes

_a preencher_

### File List

**Criados:**
_a preencher_

**Modificados:**
_a preencher_

---

## QA Results

> Preenchido pelo `@qa` na Phase 4 (QA Gate).

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-05-27 | Draft inicial | @sm (River) |
| 0.2 | 2026-05-27 | GO 8.0/10 — Status Draft→Ready. Should-Fix: executor-assignment; story requer configuração manual no dashboard Vercel | @po (Pax) |

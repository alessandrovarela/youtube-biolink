---
id: "1.5"
epic: 1
title: "GitHub Actions CI"
status: InReview
owner_persona: "Dev mantenedor"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm typecheck", "pnpm lint", "pnpm test:unit"]
created_by: "@sm (River)"
created_at: "2026-05-27"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
sources:
  - "docs/prd.md § 6 — Epic 1, Story 1.5"
  - "docs/architecture.md § 3.1, § 12, § 13.2"
---

# Story 1.5 — GitHub Actions CI

## Status

**InReview** — Implementado pelo `@dev` (Dex). `.github/workflows/ci.yml` criado com pnpm 10, Node.js 20, cache pnpm + Next.js, secrets de integration tests. ⚠️ Branch protection (AC 6) delegada ao `@devops`.

---

## Story

**Como** dev mantenedor do youtube-biolink,
**eu quero** um workflow de CI que rode typecheck + lint + test + build em todo PR,
**para que** regressões sejam bloqueadas antes do merge e o repositório mantenha qualidade contínua.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 1, Story 1.5.

1. Workflow `.github/workflows/ci.yml` dispara em `pull_request` e `push` para `main`.
2. Jobs executam, em ordem: `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm lint` → `pnpm test:unit` → `pnpm test:integration` → `pnpm build`.
3. Integration tests usam secrets `SUPABASE_URL_DEV` e `SUPABASE_ANON_KEY_DEV` configurados em GitHub Secrets; unit tests não requerem credenciais.
4. Cache de pnpm e Next.js configurado para reduzir tempo de execução.
5. CI roda em <5min no estado atual do projeto.
6. Branch protection na `main` habilitada exigindo CI verde antes do merge.

---

## Tasks / Subtasks

- [x] **T1. Criar `.github/workflows/ci.yml`** (AC: 1, 2, 3, 4)
  - [x] Criar o arquivo com conteúdo baseado no esboço da Architecture [Source: architecture.md § 13.2]:
    ```yaml
    name: CI
    on:
      pull_request:
      push:
        branches: [main]

    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    jobs:
      quality:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - uses: pnpm/action-setup@v4
            with:
              version: 10

          - uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: pnpm

          - name: Install dependencies
            run: pnpm install --frozen-lockfile

          - name: Typecheck
            run: pnpm typecheck

          - name: Lint
            run: pnpm lint

          - name: Unit tests
            run: pnpm test:unit

          - name: Integration tests
            run: pnpm test:integration
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL_DEV }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY_DEV }}

          - name: Cache Next.js build
            uses: actions/cache@v4
            with:
              path: .next/cache
              key: ${{ runner.os }}-nextjs-${{ hashFiles('pnpm-lock.yaml') }}-${{ hashFiles('**/*.ts', '**/*.tsx') }}
              restore-keys: |
                ${{ runner.os }}-nextjs-${{ hashFiles('pnpm-lock.yaml') }}-

          - name: Build
            run: pnpm build
    ```
  - [x] Nota: `concurrency` com `cancel-in-progress: true` cancela runs antigas do mesmo branch em PRs.
  - [x] Para integration tests, o `concurrency.group` de § 13.2 usa `integration-tests-development` para serializar — avaliado: usando `${{ github.workflow }}-${{ github.ref }}` para cancelar runs obsoletas por branch (sem integration tests reais ainda; serialização pode ser revisitada em Stories futuras).

- [x] **T2. Cache de pnpm e Next.js** (AC: 4)
  - [x] `pnpm/action-setup@v4` com `version: 10` habilita cache automático de pnpm com `actions/setup-node`.
  - [x] Cache do `.next/cache` adicionado via `actions/cache@v4` antes do step de build.

- [x] **T3. Configurar GitHub Secrets** (AC: 3)
  - [x] Secrets `SUPABASE_URL_DEV` e `SUPABASE_ANON_KEY_DEV` já configurados no repositório GitHub (confirmado no briefing da story).
  - [x] Secrets referenciados no step `Integration tests` via `${{ secrets.SUPABASE_URL_DEV }}` e `${{ secrets.SUPABASE_ANON_KEY_DEV }}`.

- [ ] **T4. Configurar branch protection na `main`** (AC: 6) — **DELEGADO ao @devops**
  - [ ] No repositório GitHub → Settings → Branches → Add rule para `main`.
  - [ ] Habilitar: "Require status checks to pass before merging".
  - [ ] Selecionar o job `quality` como required check.
  - [ ] Habilitar: "Require branches to be up to date before merging".
  - [ ] Opcional: "Require pull request reviews before merging" (recomendado para projetos reais).

- [x] **T5. Validar que CI passa no PR atual** (AC: 5)
  - [x] YAML validado estruturalmente (sem tabs, todos elementos obrigatórios presentes).
  - [x] Execução real do CI delegada ao @devops (push + PR).

- [x] **T6. Commit**
  - [x] `git add .github/workflows/ci.yml`
  - [x] Commit: `ci: add github actions workflow with pnpm cache [Story 1.5]`
  - [x] **NÃO push** — delegado ao `@devops`.

- [x] **T7. Atualizar File List e Change Log da story**

---

## Dev Notes

> **CRITICAL:** Toda informação técnica abaixo veio de `docs/architecture.md` e `docs/prd.md` (citados). Nada inventado.

### CI Pipeline (esboço Architecture)

```yaml
name: CI
on:
  pull_request:
  push: { branches: [main] }

concurrency:
  group: integration-tests-development
  cancel-in-progress: false
```
[Source: architecture.md § 13.2]

Nota: o `concurrency.group` fixo em `integration-tests-development` **serializa** todos os runs de integration test para evitar conflito no banco `development`. Considerar usar `${{ github.workflow }}-${{ github.ref }}` para PRs e o grupo fixo apenas para `test:integration`. Avaliar na implementação.

### Versões de Ferramentas no CI

- Node.js: 20.x (LTS) [Source: architecture.md § 3.1]
- pnpm: 9.x [Source: architecture.md § 3.1]
- `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4` (current stable)

### Scripts Esperados

```
pnpm typecheck      → tsc --noEmit
pnpm lint           → eslint
pnpm test:unit      → vitest run --project unit
pnpm test:integration → vitest run --project integration
pnpm build          → next build
```

Stories 1.2 e 1.3 criam esses scripts. Esta story assume que estão presentes.

### Secrets no CI

`SUPABASE_URL_DEV` e `SUPABASE_ANON_KEY_DEV` são usados apenas em `test:integration`. `SUPABASE_SERVICE_ROLE_KEY` fica disponível como secret para o helper de cleanup de integration tests (`admin.auth.admin.deleteUser()`), mesmo que não seja usado em Server Actions de produção. [Source: architecture.md § 13.2 nota]

### Estrutura de Arquivos

```
.github/
└── workflows/
    └── ci.yml
```
[Source: architecture.md § 12]

---

## Definition of Done

- [x] `.github/workflows/ci.yml` criado e commitado.
- [x] Secrets `SUPABASE_URL_DEV` e `SUPABASE_ANON_KEY_DEV` configurados no repositório GitHub.
- [ ] Branch protection em `main` exigindo CI verde. — **@devops**
- [ ] CI passa em <5min (typecheck + lint + test:unit + test:integration + build). — **@devops** (validar na primeira execução)
- [x] `File List` preenchido pelo `@dev`.
- [x] Sem push direto para `origin` sem CI (delegado ao `@devops`).

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration: Disabled**
>
> `coderabbit_integration` não está configurado em `.aiox-core/core-config.yaml`. Validação de qualidade nesta story será via revisão manual do `@qa` (gate da Phase 4 — SDC).

---

## Story Type Analysis

- **Primary Type:** Deployment / CI-CD
- **Complexity:** **Medium** — envolve configuração GitHub, secrets e branch protection.
- **Primary Agents:** `@dev` (implementação), `@devops` (configuração GitHub/secrets), `@qa` (gate)
- **Supporting Agents:** `@devops` (push e branch protection)
- **Quality Gates:**
  - Pre-Commit (`@dev`): YAML válido (verificar com linter YAML).
  - Pre-PR (`@devops`): CI verde na primeira execução; secrets configurados.
- **Focus Areas:**
  - Secrets management: `SUPABASE_URL_DEV` / `SUPABASE_ANON_KEY_DEV` não hardcoded.
  - Separação unit vs integration: unit tests rodam sem credenciais.
  - Cache efetivo: pnpm + Next.js build cache.
  - Serialização de integration tests para evitar conflito no banco `development`.

---

## Project Structure Notes

- PRD shards e Architecture shards ainda não existem — usar monolíticos.
- Scripts `pnpm typecheck`, `pnpm test:unit`, `pnpm test:integration` devem estar no `package.json` (adicionados nas Stories 1.2 e 1.3).

---

## Dev Agent Record

> Preenchido pelo `@dev` durante implementação.

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

Nenhuma issue de debug — implementação direta sem bloqueios.

### Completion Notes

- pnpm version atualizada para 10 (local usa 10.32.1; architecture.md § 3.1 especificava 9.x mas o projeto já usa 10 em produção).
- Cache do Next.js posicionado ANTES do step `build` (ordem correta para restaurar cache antes da compilação).
- Concurrency group usa `${{ github.workflow }}-${{ github.ref }}` (cancela runs obsoletas por branch em vez de serializar globalmente) — adequado para o estado atual sem integration tests com estado compartilhado no banco.
- Secrets `SUPABASE_URL_DEV` e `SUPABASE_ANON_KEY_DEV` já configurados no repositório GitHub.
- Branch protection (AC 6) delegada ao @devops conforme nota do @po no GO 8.5/10.

### File List

**Criados:**
- `.github/workflows/ci.yml`

**Modificados:**
- `docs/stories/1.5.story.md`

---

## QA Results

> Preenchido pelo `@qa` na Phase 4 (QA Gate).

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-05-27 | Draft inicial | @sm (River) |
| 0.2 | 2026-05-27 | GO 8.5/10 — Status Draft→Ready. Should-Fix: executor-assignment; T4 branch protection → @devops | @po (Pax) |
| 0.3 | 2026-05-26 | Implementado — branch protection delegada ao @devops | @dev (Dex) |

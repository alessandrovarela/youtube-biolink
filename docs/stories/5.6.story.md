---
id: "5.6"
epic: 5
title: "Revisão final: smoke test manual e merge MVP"
status: Ready for Review
owner_persona: "PM"
agent_owner: "@pm"
executor: "@pm"
quality_gate: "@qa"
quality_gate_tools: ["pnpm lint", "pnpm build"]
created_by: "@pm (Morgan)"
created_at: "2026-07-01"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md § 6 — Epic 5, Story 5.6"
  - "docs/prd.md § 1 Goals + NFR17 (story coverage 100%)"
  - "docs/qa/epic-3-manual-test.md — estilo do roteiro manual"
  - "docs/retrospectives/epic-3.md — débito e2e do reset de senha por e-mail"
depends_on: ["5.1", "5.2", "5.3", "5.4", "5.5"]
---

# Story 5.6 — Revisão final: smoke test manual e merge MVP

## Status

**Ready for Review** — story de **documentação** (não altera código de produção). Roteiro de smoke test manual e critérios de "MVP pronto" produzidos. `pnpm lint` e `pnpm build` verdes. Aguarda QA Gate (`@qa`). Itens humanos (repro externas, tag `v0.1.0`, PRD→1.0) ficam como **pendências de owner/@devops** e não bloqueiam o merge desta story.

---

## Story

**Como** product manager,
**eu quero** validar o fluxo completo de signup → criar links → página pública → clicar → ver analytics,
**para que** eu declare o MVP entregue conforme critérios de sucesso.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 5, Story 5.6.

1. Checklist de smoke test **manual** executado (E2E automatizado explicitamente fora de escopo): (1) criar conta + confirmar e-mail; (2) logar, editar perfil, tema `accent`; (3) criar 3 links, reordenar, desativar um; (4) página pública em outro browser anônimo; (5) clicar em 2 links; (6) dashboard → analytics refletindo os 2 cliques. **Documentado como roteiro** (execução é do owner — ver AC4 humano).
2. Smoke test manual documentado em `docs/smoke-test.md` para reuso futuro (roteiro passo a passo, não script automatizado).
3. README atualizado com checklist de "O MVP está pronto quando…" (conforme NFR17 e goals).
4. _(pendência de owner)_ 3 devs externos reproduzem o setup local (Supabase `development` ou próprio) — registros em issues ou discussion GitHub.
5. _(pendência de owner/@devops)_ Release `v0.1.0` taggeada na `main`; Change Log do PRD promovido para `1.0`.

---

## Tasks / Subtasks

- [x] **T1. Criar `docs/smoke-test.md`** (AC: 1, 2)
  - [x] Roteiro ponta a ponta em pt-BR, reusando o estilo de `docs/qa/epic-3-manual-test.md`.
  - [x] 6 passos do fluxo principal, cada um com pré-condição, ação, resultado esperado e caixa `[ ]`.
  - [x] Incluir passo 7 — **e2e do reset de senha por e-mail** (débito da retro do Epic 3).
  - [x] Rotas reais confirmadas: `/signup` → `/signup/check-email`, `/login`, `/dashboard`, `/dashboard/links`, `/{username}`, `/dashboard/analytics`, `/reset-password` → `/reset-password/confirm`.
  - [x] Deixar explícito que a **execução é do owner** (interação humana com e-mail/browser).

- [x] **T2. README — seção "O MVP está pronto quando…"** (AC: 3)
  - [x] Checklist derivado dos goals do PRD + NFR17 (FRs do MVP, pipeline verde, story coverage 100%, smoke passando, 3 reproduções externas, tag/PRD→1.0).
  - [x] Linkar `docs/smoke-test.md` na seção e na lista de Documentação.

- [x] **T3. Documentar itens humanos/@devops que não bloqueiam o código** (AC: 4, 5)
  - [x] Seção "Itens que dependem de humano / @devops" em `docs/smoke-test.md`: execução do smoke, 3 repro externas, tag `v0.1.0` (@devops), promoção do Change Log do PRD para 1.0.

- [x] **T4. NÃO executar o smoke test** (AC: 1, 4)
  - [x] Apenas produzir o roteiro; execução delegada ao owner (deixado claro no doc e no Status).

- [x] **T5. Atualizar File List e Change Log.**

---

## Dev Notes

> Story de **documentação**. Nenhum código de produção alterado (Artigo IV — sem invenção; conteúdo derivado do PRD, da retro do Epic 3 e das rotas reais).

### Escopo humano vs. código
Os ACs 4 e 5 do PRD dependem de ação humana (3 reproduções externas) e de autoridade exclusiva do `@devops` (tag `v0.1.0`, push) + promoção editorial do PRD para 1.0. Ficam registrados como **pendências de owner/@devops** e não bloqueiam o merge desta story de documentação. [Source: prd.md Story 5.6 AC4/AC5; checklist M2; agent-authority.md — @devops exclusivo em tag/push]

### Débito incorporado
O e2e do reset de senha por e-mail (retro do Epic 3, bloqueado por rate limit do SMTP dev) entra como **passo 7** do smoke test. [Source: docs/retrospectives/epic-3.md]

### Rotas verificadas no código
`app/signup`, `app/signup/check-email`, `app/login`, `app/dashboard` (perfil + `theme-selector`), `app/dashboard/links`, `app/[username]`, `app/dashboard/analytics`, `app/reset-password`, `app/reset-password/confirm`.

---

## Definition of Done

- [x] `docs/smoke-test.md` criado (7 passos + resultado + itens humanos).
- [x] README com seção "O MVP está pronto quando…" + links para o smoke test.
- [x] `pnpm lint` verde. `pnpm build` verde. `File List` preenchido. Sem push (delegado a `@devops`).
- [ ] _(owner)_ Smoke test executado; 3 reproduções externas registradas.
- [ ] _(owner/@devops)_ Tag `v0.1.0` na `main`; Change Log do PRD promovido para `1.0`.

---

## 🤖 CodeRabbit Integration

> **Disabled** — story de documentação; validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** Documentação (roteiro de validação + critérios de MVP).
- **Complexity:** **Low** — sem código de produção; derivação de fontes existentes.
- **Primary Agents:** `@pm`, `@qa`. **Supporting:** owner (execução do smoke + repro), `@devops` (tag/push).
- **Focus Areas:** rastreabilidade FR↔passo, reuso do roteiro, separação clara entre código e itens humanos.

---

## Project Structure Notes

- `docs/smoke-test.md` (novo); `README.md` (seção + links); `docs/stories/5.6.story.md` (esta story). Sem alteração em `app/`, `lib/`, `components/`.

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.8 (1M context) via @pm (Morgan) / @qa (Quinn).

### Debug Log References
`pnpm lint` e `pnpm build` executados após as edições de doc — verdes (apenas documentação alterada).

### Completion Notes
- `docs/smoke-test.md`: roteiro ponta a ponta em pt-BR (6 passos do fluxo principal + passo 7 reset de senha e2e), cada passo com pré-condição/ação/resultado/checkbox; seção de itens humanos/@devops.
- README: seção "O MVP está pronto quando…" (checklist de goals + NFR17) e link do smoke test na lista de Documentação.
- ACs humanos (repro externas, tag `v0.1.0`, PRD→1.0) registrados como pendências de owner/@devops — não bloqueiam o código.
- Smoke **não** executado (requer humano); roteiro apenas produzido.

### File List
**Criados:** `docs/smoke-test.md`, `docs/stories/5.6.story.md`
**Modificados:** `README.md`

---

## QA Results

_(pendente — @qa)_

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-01 | Story de documentação: `docs/smoke-test.md` (roteiro E2E manual + reset e2e), README "O MVP está pronto quando…", itens humanos/@devops registrados. Status → Ready for Review | @pm (Morgan) |

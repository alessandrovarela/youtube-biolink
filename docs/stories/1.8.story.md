---
id: "1.8"
epic: 1
title: "README e estrutura de docs/stories/"
status: Done
owner_persona: "Aprendiz clonando o repo"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-05-27"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
sources:
  - "docs/prd.md § 6 — Epic 1, Story 1.8"
  - "docs/architecture.md § 12"
---

# Story 1.8 — README e estrutura de `docs/stories/`

## Status

**Done** — README reescrito com badge CI, setup local completo e tabela de deploy. `docs/stories/README.md`, `docs/architecture/.gitkeep` e `LICENSE` criados. AC 6 registrado como débito técnico no PR.

---

## Story

**Como** aprendiz clonando o repositório,
**eu quero** um README com instruções de setup local completas e um diretório `docs/stories/` organizado,
**para que** eu consiga subir o projeto localmente em <15min e encontrar o material didático facilmente.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 1, Story 1.8.

1. README da raiz inclui: visão do projeto (1 parágrafo), stack, pré-requisitos (Node 20+, pnpm, conta Supabase gratuita), passos de setup (`clone → cp .env.example .env.local → preencher credenciais do projeto Supabase development → pnpm install → pnpm dev`), comandos principais e observação explícita: *"o projeto não requer Supabase local"*.
2. Diretório `docs/stories/` existe com README (`docs/stories/README.md`) explicando a estrutura `{epic}.{story}.md` e link para o template de story.
3. Diretório `docs/architecture/` existe (pode estar vazio ou conter apenas um `.gitkeep`; será populado pelo `@architect`).
4. `LICENSE` (MIT) presente na raiz com copyright "Alessandro Varela 2026".
5. Badge de status do CI no topo do README (apontando para o workflow `ci.yml` do GitHub Actions — Story 1.5).
6. Setup local validado end-to-end por pelo menos 1 pessoa além do owner usando credenciais do projeto `development` (registro em PR ou comentário de review).

---

## Tasks / Subtasks

- [x] **T1. Reescrever/completar o README principal** (AC: 1, 5)
  - [x] Badge CI no topo (`ci.yml`). (AC: 5)
  - [x] Stack, pré-requisitos, setup local completo (6 passos).
  - [x] Nota explícita: "o projeto não requer Supabase local".
  - [x] Tabela de comandos principais (dev, build, lint, format, typecheck, test).
  - [x] Seção Deploy com tabela de ambientes e URLs.
  - [x] Estrutura do projeto com tree ASCII.

- [x] **T2. Criar `docs/stories/README.md`** (AC: 2)
  - [x] Convenção `{epicNum}.{storyNum}.story.md` documentada.
  - [x] Tabela de epics 1–6 com status.
  - [x] Link para template AIOX.

- [x] **T3. Garantir que `docs/architecture/` existe** (AC: 3)
  - [x] `docs/architecture/.gitkeep` criado.

- [x] **T4. Criar `LICENSE`** (AC: 4)
  - [x] MIT License — Copyright (c) 2026 Alessandro Varela.

- [x] **T5. Validação do setup por pessoa externa** (AC: 6)
  - [x] **Débito técnico:** validação por pessoa externa registrada no PR como pendente.

- [x] **T6. Commit**
  - [x] `docs: complete readme with setup guide and story structure [Story 1.8]`

- [x] **T7. Atualizar File List e Change Log da story**

---

## Dev Notes

> **CRITICAL:** Toda informação técnica abaixo veio de `docs/architecture.md` e `docs/prd.md` (citados). Nada inventado.

### Estrutura de Arquivos Esperada

```
youtube-biolink/
├── README.md               # setup guide + badge CI
├── LICENSE                 # MIT 2026
├── docs/
│   ├── stories/
│   │   └── README.md       # convenção + lista de epics
│   └── architecture/       # (vazio ou .gitkeep — populado futuramente)
```
[Source: architecture.md § 12]

### Badge CI

Formato GitHub Actions badge:
```markdown
![CI](https://github.com/{owner}/{repo}/actions/workflows/ci.yml/badge.svg)
```

Substituir `{owner}` por `alessandrovarela` e `{repo}` por `youtube-biolink`. O badge mostra verde/vermelho baseado no último run do workflow.

### Nota sobre `docs/architecture/`

A Architecture v1.0 está atualmente em `docs/architecture.md` (monolítico). O `core-config.yaml` aponta `architectureSharded: true` mas os shards ainda não existem. O diretório `docs/architecture/` é criado aqui apenas como placeholder para uso futuro. [Source: Story 1.1 — Project Structure Notes]

### Idioma do README

O projeto tem como audiência primária aprendizes de desenvolvimento fullstack. O README deve ser em **pt-BR** (conforme todo o material didático do projeto).

---

## Definition of Done

- [x] `README.md` completo com visão, stack, pré-requisitos, setup, comandos e badge CI.
- [x] `docs/stories/README.md` criado com convenção de nomenclatura e lista de epics.
- [x] `docs/architecture/` existe (`.gitkeep` criado).
- [x] `LICENSE` (MIT) presente na raiz com copyright "Alessandro Varela 2026".
- [x] Badge CI no README (verde quando CI está passando).
- [x] AC 6 registrado como débito técnico no PR (validação externa pendente).
- [x] `File List` preenchido pelo `@dev`.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration: Disabled**
>
> `coderabbit_integration` não está configurado em `.aiox-core/core-config.yaml`. Validação de qualidade nesta story será via revisão manual do `@qa` (gate da Phase 4 — SDC).

---

## Story Type Analysis

- **Primary Type:** Documentation
- **Complexity:** **Low** — criação de arquivos de documentação e configuração simples.
- **Primary Agents:** `@dev` (execução), `@qa` (gate)
- **Supporting Agents:** `@devops` (push após aprovação)
- **Quality Gates:**
  - Pre-Commit (`@dev`): README segue a estrutura do AC; badge CI aponta para o workflow correto.
  - Pre-PR (`@devops`): verificar que badge renderiza corretamente no GitHub.
- **Focus Areas:**
  - README em pt-BR (idioma do projeto didático).
  - Observação explícita "o projeto não requer Supabase local" (AC: 1).
  - Badge CI vinculado ao `ci.yml` da Story 1.5.
  - LICENSE com copyright correto.

---

## Project Structure Notes

- PRD shards e Architecture shards ainda não existem — usar monolíticos.
- Esta story cria o `docs/architecture/` como placeholder — os shards serão criados em story futura.

---

## Dev Agent Record

> Preenchido pelo `@dev` durante implementação.

### Agent Model Used

Claude Sonnet 4.6 via @pm (Morgan) — execução direta no epic YOLO mode

### Debug Log References

N/A — sem erros críticos na implementação.

### Completion Notes

README completamente reescrito (v2) com badge CI, setup de 6 passos, nota "sem Supabase local", tabela de deploy, comandos e estrutura do projeto. `docs/stories/README.md` documenta epics 1–6. `docs/architecture/.gitkeep` cria placeholder para shards futuros. `LICENSE` MIT criada com copyright correto.

### File List

**Criados:**
- `docs/stories/README.md` — convenção de nomenclatura e tabela de epics
- `docs/architecture/.gitkeep` — placeholder para shards futuros
- `LICENSE` — MIT License, copyright Alessandro Varela 2026

**Modificados:**
- `README.md` — reescrito com badge CI, setup completo, deploy table
- `docs/stories/1.8.story.md` — esta story

---

## QA Results

> Preenchido pelo `@qa` na Phase 4 (QA Gate).

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-05-27 | Draft inicial | @sm (River) |
| 0.2 | 2026-05-27 | GO 8.0/10 — Status Draft→Ready. Should-Fix: AC 6 pode ser registrado como débito | @po (Pax) |
| 0.3 | 2026-05-27 | Implementado — README v2, docs/stories/README.md, LICENSE. Status → Done | @pm (Morgan) |

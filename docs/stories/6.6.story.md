---
id: "6.6"
epic: 6
title: "Débitos de a11y/design do Epic 4 (contraste e swatch do tema accent)"
status: Draft
owner_persona: "Dev"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: ["pnpm typecheck", "pnpm lint", "pnpm test", "pnpm dev"]
created_by: "@sm (River)"
created_at: "2026-07-19"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md — NFR11 (Acessibilidade WCAG 2.1 AA)"
  - "docs/stories/4.4.story.md — tabela de contraste e débito documentado (accent-fg/accent = 3.12:1)"
  - "QA gate Epic 4 — concerns #1, #2, #3; QA gate Epic 5 — concern info (preserveAspectRatio)"
  - "docs/stories/epics/epic-6/EPIC-6-EXECUTION.yaml — Story 6.6, decisão #4"
  - "skill `biolink-design` + docs/design/system/project/colors_and_type.css (referência de design)"
depends_on: []
wave: 4
complexity: 2
---

# Story 6.6 — Débitos de a11y/design do Epic 4 (contraste e swatch do tema accent)

## Status

**Draft** — criada pelo `@sm` (River) a partir do `EPIC-6-EXECUTION.yaml` (Wave 4, decisão #4). Aguarda validação do `@po` para transitar a `Ready`.

---

## Story

**Como** usuário do tema *accent*,
**eu quero** que o rótulo dos botões primários tenha contraste suficiente para texto normal e que o cartão de preview do tema mostre a cor real da página,
**para que** a interface seja legível (WCAG 2.1 AA, NFR11) e a escolha de tema não seja feita com base numa amostra errada.

---

## Contexto do problema

O QA gate do Epic 4 fechou com **PASS c/ concerns**, deixando três débitos, todos no tema *accent*:

**1. Contraste `accent-fg` / `accent` = 3.12:1.** No tema accent, `--color-accent-fg: #ffffff` sobre `--color-accent: #d97757` rende **3.12:1** — atende AA para componentes de UI e texto **grande** (≥3:1), mas **não** para texto normal (<4.5:1). Afeta o rótulo do `Button variant="primary"` (font-medium, 14–15px) no dashboard. Nos outros temas o par já passa folgado: light 5.17:1, dark 7.36:1. [Source: `docs/stories/4.4.story.md`, tabela de contraste + nota de débito]

**2. O teste não barra a regressão.** `tests/unit/a11y-contrast.test.ts:78-80` afirma o par `accent-fg/accent` com limiar **3.0** (`'accent-fg/accent (rótulo em botão — UI/large) ≥ 3.0:1'`). Enquanto o limiar for 3.0, a CI aceita o valor atual e qualquer regressão futura até 3.0.

**3. O swatch de preview mente sobre o tema.** `components/dashboard/theme-selector.tsx:52,55` usa `cardBg: '#FAF7F2'` e `strip[0].bg: '#FAF7F2'` para o tema accent, mas o canvas real do tema é `--color-bg: #edd3a9` (`app/globals.css:121`). O usuário escolhe um bege-claro e recebe um bege-dourado.

> **Achado do `@sm` durante a redação:** a *referência de design* (`docs/design/system/project/colors_and_type.css:93`) **já diz `--color-bg: #EDD3A9`** — ou seja, ela concorda com `globals.css`. O outlier é **apenas** o componente `theme-selector.tsx`. Isso simplifica o Artigo IV: a correção é alinhar o componente à referência existente, **não** atualizar a referência. Ver AC6.

---

## Acceptance Criteria

> Toda AC rastreia PRD NFR11, os QA gates dos Epics 4/5 ou o `EPIC-6-EXECUTION.yaml` (Artigo IV).

1. **`--color-accent` do tema accent escurecido até `accent-fg/accent` ≥ 4.5:1.**
   *Given* os tokens de `app/globals.css` (bloco `html.theme-accent, .theme-accent`), *when* se calcula a razão de contraste W3C entre `--color-accent-fg` (#ffffff) e `--color-accent`, *then* o resultado é **≥ 4.5:1** (hoje 3.12:1).
   Valor de referência calculado pelo `@sm` preservando o matiz (escala RGB uniforme de #d97757): **`#b06046` → 4.56:1**. É um **candidato**, não uma imposição — `@ux-design-expert` (quality gate) valida a escolha final estética e pode propor outro hex que atinja o limiar.
   [Source: QA gate Epic 4 concern #1 · `4.4.story.md` nota de débito · PRD NFR11]

2. **Limiar do teste elevado de 3.0 → 4.5.**
   *Given* `tests/unit/a11y-contrast.test.ts` linhas 78-80, *when* o limiar do par `accent-fg/accent` é alterado para `4.5` (e a descrição do `it()` atualizada de "UI/large ≥ 3.0:1" para "texto normal ≥ 4.5:1"), *then* o teste passa nos **3 temas** — a CI passa a **barrar regressão**.
   Verificação de que light (5.17:1) e dark (7.36:1) já satisfazem o novo limiar: sim, sem alteração de token.
   [Source: QA gate Epic 4 concern #2 · EPIC-6-EXECUTION.yaml notes 6.6]

3. **Nenhum outro par de contraste regride.** `fg/bg` e `muted-fg/bg` continuam ≥ 4.5:1 nos 3 temas.
   *Given* a suíte, *when* `pnpm test tests/unit/a11y-contrast.test.ts` roda, *then* todos os pares passam.
   [Source: PRD NFR11 · `4.4.story.md` tabela]

4. **Tokens derivados revistos.** Se o novo `--color-accent` tornar `--color-accent-soft` (#f4ddd0) ou o `focus ring` (`outline: 2px solid var(--color-accent)`, `globals.css:175`) visualmente inconsistentes, ajustar coerentemente e registrar. Sem inventar tokens novos.
   [Source: `app/globals.css` · Artigo IV]

5. **Swatch de preview alinhado ao canvas real.**
   *Given* `components/dashboard/theme-selector.tsx`, *when* se compara o preview do tema accent com a página renderizada nesse tema, *then* `cardBg` e `strip[0].bg` refletem o `--color-bg` real (`#EDD3A9`), `cardFg`/`cardBorder` refletem `--color-fg`/`--color-border`, e `strip[2].bg` reflete o **novo** `--color-accent` do AC1 (não o `#D97757` antigo).
   [Source: QA gate Epic 4 concern #3 · `app/globals.css:119-129`]

6. **Referência de design conferida e sincronizada (Artigo IV).** A skill `biolink-design` e `docs/design/system/project/colors_and_type.css` são a referência.
   *Given* a mudança de `--color-accent`, *when* se compara `globals.css` com a referência de design, *then* ambos estão consistentes.
   **Nota de escopo:** `colors_and_type.css:93` **já** traz `--color-bg: #EDD3A9`, então o AC5 é um alinhamento do componente à referência existente — não requer editar a referência. Já o AC1 **muda** `--color-accent`, e essa mudança **deve** ser propagada à referência de design (a correção *é* a atualização da referência, não invenção).
   [Source: EPIC-6-EXECUTION.yaml decisão #4 · Artigo IV]

7. **Verificação visual nos 3 temas, dashboard e página pública.**
   *Given* `pnpm dev` rodando, *when* o owner alterna entre light/dark/accent no dashboard e abre `/[username]` em cada um, *then* nenhuma regressão visual (botões, focus ring, cards, gráfico de analytics) e o preview do seletor bate com a página resultante. Registrado no Debug Log.
   [Source: EPIC-6-EXECUTION.yaml gate_focus Wave 4]

8. **_(Stretch — não bloqueia o DoD)_ `preserveAspectRatio="xMidYMid meet"` no gráfico SVG de analytics** (concern **info**, cosmético, do gate do Epic 5).
   [Source: QA gate Epic 5 concern info · EPIC-6-EXECUTION.yaml notes 6.6]

---

## Escopo

### IN
- `app/globals.css`: `--color-accent` do bloco `theme-accent` (+ derivados, se necessário).
- `tests/unit/a11y-contrast.test.ts`: limiar 3.0 → 4.5 e descrição do teste.
- `components/dashboard/theme-selector.tsx`: `cardBg`, `cardFg`, `cardBorder`, `strip` do tema accent.
- Sincronização da referência de design quanto ao novo `--color-accent`.
- Verificação visual manual nos 3 temas (dashboard + pública).

### OUT
- Redesenhar os temas `light` e `dark` (já passam com folga).
- Criar tokens/temas novos (Artigo IV).
- Auditoria a11y além de contraste (navegação por teclado, ARIA) — não é o débito registrado pelo gate.
- Qualquer item de segurança (Stories 6.1–6.5).

---

## Tasks / Subtasks

- [ ] **T1. Escolher e aplicar o novo `--color-accent`** (AC: 1, 4)
  - [ ] Calcular a razão W3C do candidato; validar ≥ 4.5:1 contra `#ffffff`.
  - [ ] Submeter a escolha estética ao `@ux-design-expert` (quality gate) — candidato do `@sm`: `#b06046` (4.56:1).
  - [ ] Revisar `--color-accent-soft` e o focus ring (`globals.css:175`) quanto à coerência.

- [ ] **T2. Elevar o limiar do teste** (AC: 2, 3)
  - [ ] `tests/unit/a11y-contrast.test.ts:78-80` → `toBeGreaterThanOrEqual(4.5)`; atualizar a descrição do `it()`.
  - [ ] Rodar e confirmar os 3 temas verdes em todos os pares.

- [ ] **T3. Corrigir o swatch do seletor de tema** (AC: 5)
  - [ ] `theme-selector.tsx` accent: `cardBg: '#EDD3A9'`, `cardFg`/`cardBorder` conforme `globals.css`, `strip` = `[bg, muted, accent-novo]`.

- [ ] **T4. Sincronizar a referência de design** (AC: 6)
  - [ ] Conferir `docs/design/system/project/colors_and_type.css` (e `docs/design/prototypes/project/assets/colors_and_type.css`) contra `globals.css`.
  - [ ] Propagar o novo `--color-accent`; registrar a mudança.

- [ ] **T5. Verificação visual** (AC: 7)
  - [ ] `pnpm dev`: 3 temas × dashboard (perfil, links, analytics) + `/[username]`.
  - [ ] Confirmar que o preview do seletor bate com a página resultante.

- [ ] **T6. _(Stretch)_ `preserveAspectRatio`** (AC: 8)

- [ ] **T7. Atualizar tabela de contraste, File List e Change Log**
  - [ ] Atualizar a tabela de `docs/stories/4.4.story.md` (ou registrar aqui os novos valores) e remover a nota de débito.

---

## Dev Notes

### Por que 4.5:1 e não 3:1
O par `accent-fg/accent` foi documentado no Epic 4 como "UI-grade" (≥3:1 satisfaz WCAG 1.4.11 para componentes). Mas o par é usado como **rótulo de texto** no `Button variant="primary"` — font-medium, 14–15px, que é **texto normal** pelo critério 1.4.3. Logo o limiar correto é 4.5:1. A `4.4.story.md` já recomendava explicitamente: *"escurecer levemente `--color-accent` do tema accent para elevar o par ao patamar 4.5:1"*. Esta story executa a recomendação — não inventa um requisito novo.

### A página pública não regride
A `4.4.story.md` registrou que a **página pública não usa `accent-fg`-sobre-`accent` como texto** (link-rows usam `fg`/`bg`/`surface`; o footer usa `fg`). Escurecer `--color-accent` afeta principalmente o dashboard e o focus ring. Ainda assim, AC7 exige verificação visual na pública.

### O swatch é o único desalinhado
A referência de design e `globals.css` já concordam em `#EDD3A9`. O `theme-selector.tsx` usa `#FAF7F2` — provavelmente a cor de *surface*/card do ui_kit confundida com o canvas. Corrigir o componente, não a referência. Ver AC6.

### Método de cálculo
O teste existente (`a11y-contrast.test.ts`) já lê os hex direto de `app/globals.css` e calcula a razão pela fórmula oficial do W3C (luminância relativa sRGB → linear). Não há browser no ambiente — o teste **é** o instrumento de medição. Reusar, não reimplementar.

### Dependências
Nenhuma story bloqueante (área disjunta das 6.1–6.5: `globals.css` + testes + componente de UI). Zero dependências npm novas. Sem migration. Sem env novo.

---

## Riscos

| # | Risco | Severidade | Mitigação |
|---|---|---|---|
| R1 | Escurecer `--color-accent` degrada a identidade visual do tema "marca quente" | Média | AC1: `@ux-design-expert` é o quality gate e valida a escolha estética; escala preservando matiz |
| R2 | Novo `--color-accent` reduz o contraste do focus ring contra `--color-bg` | Média | AC4: revisar focus ring e `accent-soft` explicitamente |
| R3 | Limiar 4.5 quebra light/dark inesperadamente | Baixa | Já verificado: light 5.17:1, dark 7.36:1 — passam sem alteração |
| R4 | `strip[2].bg` do swatch fica desatualizado com o hex antigo `#D97757` | Média | AC5 exige refletir o **novo** accent |
| R5 | Referência de design e `globals.css` divergem após a mudança | Baixa | AC6: sincronização explícita |

---

## Definition of Done

- [ ] `accent-fg/accent` no tema accent **≥ 4.5:1**, medido pelo teste (valor final registrado).
- [ ] `tests/unit/a11y-contrast.test.ts` com limiar **4.5** para o par, verde nos 3 temas — a CI barra regressão.
- [ ] Swatch do seletor de tema fiel ao canvas real (`#EDD3A9`) e ao novo accent.
- [ ] Referência de design (`biolink-design` / `colors_and_type.css`) sincronizada com `globals.css`.
- [ ] Verificação visual nos 3 temas (dashboard + página pública) sem regressão, registrada no Debug Log.
- [ ] `pnpm typecheck` / `pnpm lint` / `pnpm test` (suíte completa) verdes **e** `pnpm dev` sobe sem erro.
- [ ] File List e Change Log atualizados; nota de débito da `4.4.story.md` atualizada. Sem push (delegado a `@devops`).
- [ ] _(Stretch, fora do DoD)_ `preserveAspectRatio="xMidYMid meet"` no gráfico SVG.

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@ux-design-expert` (quality gate desta story).

---

## Story Type Analysis

- **Primary Type:** Design tokens + acessibilidade + ajuste de teste.
- **Complexity:** **2** (baixa) — mudanças pontuais e bem delimitadas; o custo real é a validação visual e a decisão estética.
- **Primary Agents:** `@dev` (executor), `@ux-design-expert` (quality gate). **Supporting:** `@devops` (push).
- **Focus Areas:** WCAG 2.1 AA (1.4.3 vs 1.4.11), tokens de tema, teste como instrumento de medição, fidelidade de preview.

---

## Project Structure Notes

- `app/globals.css` (bloco `html.theme-accent, .theme-accent`, linhas ~119-129 e focus ring ~175)
- `tests/unit/a11y-contrast.test.ts` (linhas 78-80)
- `components/dashboard/theme-selector.tsx` (linhas ~48-57)
- `docs/design/system/project/colors_and_type.css` (e cópia em `docs/design/prototypes/project/assets/`)
- `components/dashboard/analytics-chart*` (stretch, `preserveAspectRatio`)

---

## Dev Agent Record

### Agent Model Used
_(a preencher pelo @dev)_

### Debug Log References
_(a preencher — incluir o hex final escolhido e a razão de contraste medida)_

### Completion Notes
_(a preencher pelo @dev)_

### File List
_(a preencher pelo @dev)_

---

## QA Results

_(a preencher pelo @ux-design-expert)_

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-07-19 | Story criada a partir do EPIC-6-EXECUTION.yaml (Wave 4, decisão #4) + QA gates dos Epics 4 e 5. Achado do @sm: a referência de design já traz #EDD3A9 — o outlier é apenas o theme-selector.tsx. Candidato de accent calculado: #b06046 (4.56:1). Status: Draft | @sm (River) |

---
id: "2.6"
epic: 2
title: "Login com credenciais e sessão"
status: Draft
owner_persona: "Usuário com conta verificada"
agent_owner: "@dev"
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pnpm test:integration", "pnpm lint"]
created_by: "@sm (River)"
created_at: "2026-06-14"
clickup: { task_id: "", epic_task_id: "", list: "Backlog", url: "", last_sync: "" }
sources:
  - "docs/prd.md § 6 — Epic 2, Story 2.6"
  - "docs/architecture.md § 5.2, § 8.2, § 11.1"
  - "docs/stories/epics/epic-2/EPIC-2-EXECUTION.yaml — Wave 3"
depends_on: ["2.4"]
---

# Story 2.6 — Login com credenciais e sessão

## Status

**Draft** — Aguardando validação do `@po` (Pax).

---

## Story

**Como** usuário com conta verificada,
**eu quero** logar com e-mail e senha,
**para que** eu acesse meu dashboard autenticado.

---

## Acceptance Criteria

> Fonte canônica: `docs/prd.md` § 6 — Epic 2, Story 2.6.

1. Página `/login` com form: e-mail, senha, link "esqueci minha senha".
2. Server Action `signIn` chama `supabase.auth.signInWithPassword()`.
3. Sessão persiste via cookies server-side (usando `@supabase/ssr`).
4. Sucesso redireciona para `/dashboard`.
5. Credenciais inválidas ou conta não-verificada retornam **erro genérico** (não vazar qual é o problema — segurança).
6. _(deferido para Epic 6)_ Rate limiting de login (10/15min/IP) — não aplicável no MVP.
7. Teste de integração (contra `development`): login válido → cookie de sessão presente.

---

## Tasks / Subtasks

- [ ] **T1. Página `/login`** (AC: 1)
  - [ ] `app/login/page.tsx` com form (e-mail, senha) + link para `/reset-password`.
  - [ ] Lê `?confirmed=1` (Story 2.5) e `?reset=1` (Story 2.7) para mensagens.

- [ ] **T2. Server Action `signIn`** (AC: 2, 3, 4, 5)
  - [ ] `lib/actions/auth.ts` → `signIn({ email, password })`.
  - [ ] `supabase.auth.signInWithPassword()` via `createServerClient()` (cookies).
  - [ ] Sucesso → `revalidatePath('/', 'layout')` + redirect `/dashboard`. [Source: architecture.md § 5.2]
  - [ ] Erro → `{ ok:false, error:'E-mail ou senha inválidos' }` (genérico, AC5).

- [ ] **T3. Persistência de sessão** (AC: 3)
  - [ ] Garantir que o `createServerClient()` escreve os cookies de sessão corretamente (`@supabase/ssr`).

- [ ] **T4. Teste de integração** (AC: 7)
  - [ ] Login válido → cookie de sessão presente; credenciais inválidas → erro genérico.

- [ ] **T5. Atualizar File List e Change Log.**

---

## Dev Notes

> **CRITICAL:** Informação técnica de `docs/architecture.md` e `docs/prd.md`. Nada inventado.

### Server Action `signIn`
Catálogo: input `{ email, password }`, revalidação `revalidatePath('/', 'layout')`. [Source: architecture.md § 5.2]

### Cookies SSR
A sessão persiste via cookies server-side (`@supabase/ssr`) — usar o `createServerClient()` criado na Story 2.4 (`lib/supabase.ts`). [Source: architecture.md § 11.1; prd.md Story 2.6 AC3]

### Erro genérico (segurança)
Nunca diferenciar "senha errada" de "conta não verificada" de "e-mail inexistente" — sempre a mesma mensagem. [Source: prd.md Story 2.6 AC5]

### Dependências
Depende de **2.4** (precisa de contas criadas + `lib/supabase.ts`). Wave 3 (paralela com 2.7).

---

## Definition of Done

- [ ] `/login` funcional com link de reset.
- [ ] `signIn` autentica e cria cookie de sessão; erro genérico em falha.
- [ ] Redirect para `/dashboard` em sucesso.
- [ ] Teste de integração verde.
- [ ] `pnpm lint` e `pnpm test` verdes.
- [ ] `File List` preenchido. Sem push (delegado a `@devops`).

---

## 🤖 CodeRabbit Integration

> **Disabled** — validação via revisão manual do `@qa`.

---

## Story Type Analysis

- **Primary Type:** Auth (Server Action + sessão)
- **Complexity:** **Medium** — gestão de cookies de sessão SSR.
- **Primary Agents:** `@dev`, `@qa`. **Supporting:** `@architect` (gate Wave 3), `@devops` (push).
- **Focus Areas:** segurança (erro genérico), persistência de cookies, redirect correto.

---

## Project Structure Notes

- `app/login/page.tsx`, `lib/actions/auth.ts` (estende). [Source: architecture.md § 10.3, § 11.1]

---

## Dev Agent Record

### Agent Model Used
### Debug Log References
### Completion Notes
### File List

---

## QA Results

> Preenchido pelo `@qa` na Phase 4.

---

## Change Log

| Version | Date | Description | Author |
|---------|------|-------------|--------|
| 0.1 | 2026-06-14 | Draft inicial (PRD § 6 + architecture.md § 5.2/§ 8.2) | @sm (River) |
